# LaTeX auxiliary files
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
*.toc
*.lof
*.lot
*.idx
*.ilg
*.ind
*.dvi

# Compiled PDF (Overleaf compiles this — don't track)
main.pdf

# Python
__pycache__/
*.pyc

# Stata binary intermediate; tables/a7_estimates.csv is the tracked interface
tables/*.dta

# per-school derived input to ch3_appendix.do (carries gold-standard scores; the
# dataset it derives from is deliberately untracked) -- regenerate with
# build_ch3_appendix_input.py
tables/ch3_appendix_input.csv
