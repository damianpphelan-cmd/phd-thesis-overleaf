\chapter{School Culture and Pupil Progress: Evidence from English Secondary Schools}
\label{ch:paper2}
\resetcounterformats

\begin{center}
  \textit{Damian Phelan%
    \thanks{Department of Economics, University College London.
      [\textit{INSERT: acknowledgements.}]}}
\end{center}

\begin{chapterabstract}
This paper asks whether the culture of a school affects how much progress its pupils make. I measure the warmth and strictness of \NTierOne{} English secondary schools by direct observation, and relate those scores to Progress~8, the Department for Education's measure of pupil progress. The estimates are associations, with intake, school characteristics and the pre-pandemic inspection grade held constant. There are four main results. First, both dimensions are associated with progress: a standard deviation of warmth goes with 0.15 of a grade more progress, and a standard deviation of strictness with 0.11. Second, the quality of the teaching observed does not account for either association. Third, the two dimensions add rather than amplify each other, and the schools in the top half on both dimensions outperform the rest by two-fifths of a GCSE grade per subject. The warmth association also depends on the observation: substituting the headteacher's own account of the school for the scores removes it. Fourth, the results extend nationally: culture scores read from inspection reports predict progress across effectively all English state secondary schools. A standard deviation of the strictness reading goes with 0.13 of a grade, and of the warmth reading with 0.08.
\end{chapterabstract}

% ---------------------------------------------------------------
\section{Introduction}
\label{sec:p2_intro}

This paper asks whether the culture of a school affects how much progress
its pupils make. I estimate the association between the warmth and strictness of
English secondary schools, measured by direct observation, and the progress their pupils make between the end of primary school and their GCSEs. Warmth is the quality of the relationships between adults and pupils, in lessons and around the school. Strictness is the orderliness of the school and how consistently its behaviour rules are enforced. I use the enacted culture scores that were constructed in the previous chapter for the \NTierOne{} visited schools, enacted meaning built from what the observers saw rather than from what the school says about itself, together with the Progress~8 measure
published by the Department for Education, which records each school's
average pupil progress relative to pupils nationally with the same prior
attainment.

Whether the school a child attends makes a difference beyond the ability and family circumstances the child arrives with, and if so through what mechanism, is a long-standing question. The early answers were negative.
\citet{coleman1966} found that school resources explained little of the
variation in attainment once family background was taken into account, and
\citet{hanushek1997}, reviewing the evidence three decades later, found no consistent relationship between expenditure and performance.

The class-size experiments and natural experiments that followed
\citep{krueger1999,krueger2001,angrist1999} showed that resources can
matter, and English evidence finds small positive effects of spending that
are concentrated among disadvantaged pupils
\citep{levacic2005,jenkins2006,holmlund2010,machin2010}. However, these
effects are modest when set against the size of the school-to-school
variation that they leave unexplained.

Two lines of work have looked for the source of those school-to-school differences in something other than resources. \citet{rutter1979}, studying twelve inner-London
secondary schools, placed it in what they called ethos, meaning the
expectations and routine practice that a school sustains, and the
effective-schools syntheses that followed listed an orderly environment and
a supportive climate among its correlates
\citep{edmonds1979,sammons1995,teddlie2000}. The teacher-quality literature
placed it in the classroom: \citet{rivkin2005} and \citet{chetty2014} show
that variation in teacher effectiveness is large and consequential, and
English estimates \citep{slater2012,murphy2011} are of the same order. What
neither line of work settles is whether a school's culture, as distinct from
the quality of its individual teachers, predicts how much progress its pupils
make, and that is the question here.

Two strands of the literature are closest to this paper. The first is the
authoritative-school literature \citep{pellerin2005,gregory2010}, which
builds on the parenting typology of \citet{maccoby1983}, in which the combination of responsiveness and demandingness predicts the best child outcomes \citep{majumder2015,zhang2017}. Warmth and strictness are the school-level counterparts of that pair, and the literatures that follow each have their own names for the same two things: responsiveness, support and warmth on one side; demandingness, structure and discipline on the other. This chapter uses warmth and strictness throughout. The literature finds that schools which combine the two produce better behavioural and engagement outcomes, but it has rarely had an academic-progress outcome or a direct measure of both dimensions.

The second strand is the literature on high-expectation, high-structure charter and academy models, which finds
large effects of a bundle of practices. In the United States, effective
charter schools are marked by strict discipline and high expectations
\citep{abdulkadiroglu2011,angrist2010}; an index of five such practices
explains about half of the variation in charter effectiveness, while class
size and spending explain none of it \citep{dobbie2013}; and the bundle
transplanted into ordinary public schools raises mathematics attainment
\citep{fryer2014}. \citet{chabrier2016} and \citet{epple2016} review the wider evidence. Most charter schools do no better than the ordinary public schools their pupils would otherwise have attended, and the effective ones are again those with strict discipline and high expectations.

In England, the first wave of academies, which targeted under-performing schools and gave them autonomy,
raised attainment \citep{machin2011,eyles2016}. The later converter academies show much weaker effects \citep{eyles2018}. This suggests that
autonomy itself is not the operative channel. \citet{machin2020} show that the early academies' higher exclusion rates reflected a change in discipline rather than the removal of weak pupils. What the improving academies demonstrably changed, then, was behaviour, which is the strictness dimension this paper measures directly.

In all of this work, warmth and strictness are never observed separately. An effective charter school of this kind is strict and warm at once: tight behaviour rules, and adults who know every pupil individually. The studies compare schools that run the whole package with schools that do not, so they can say that the package works, but not which part of it does. This paper separates the parts. It measures warmth and strictness as two scores, in ordinary schools rather than in schools built around the package, and asks what each contributes on its own.

The estimates are associations, not causal effects, and every one is presented as such. What the design does is narrow the list of other explanations. The outcome is value-added, so the largest confound in any school comparison, that stronger schools take stronger pupils, is removed at the start. The regressions then hold constant intake composition, prior attainment, school type, size, location and the school's pre-pandemic inspection grade, so a culture coefficient cannot be a disguised effect of who attends the school or of how it was regarded before the outcome cohorts arrived. The sample excludes schools that admit pupils only at 13 or 14, whose published progress partly belongs to the schools their pupils came from. And the associations reported below appear again across the national population, on text-derived scores, so they are not a property of the hundred schools that agreed to be visited. The one thing the design cannot rule out is a persistent characteristic of a school that produces both its culture and its results.

There are four main results. First, both dimensions are associated with progress. A standard deviation of enacted warmth goes with Progress~8 that is $0.15$ of a grade higher; a standard deviation of enacted strictness goes with $0.11$ of a grade. The two estimates are close in size, and the difference between them is not statistically significant, so the data do not rank the two dimensions. Both hold however the inspection-grade control is handled.

Behind those similar sizes the two associations behave differently. The strictness association is visible with no controls at all: stricter schools simply make more progress. The warmth association is not. At first sight warmth appears unrelated to progress, and the reason is an intake pattern that hides it. Schools with many pupils who speak English as an additional language tend to make more progress, and the observers scored those schools lower on warmth. Comparing all schools together therefore mixes the warmth association with an intake pattern that pulls the opposite way, and the two roughly cancel. Compare schools with similar intakes and the warmth association emerges. Once it does, an unmeasured confounder would have to be stronger to explain it away than to explain away the strictness association.

The two dimensions also carry separate information. Entered on its own, each has a coefficient of about $0.2$. Entered together, each falls, but neither falls to zero, so each is associated with progress in a way the other does not capture.

Second, conditioning on enacted teaching quality does not account for the association. Adding the visit's teaching score leaves both culture coefficients essentially unchanged, and the teaching coefficient itself is close to zero. This matters because teaching quality is the most obvious rival explanation: a warm and orderly school might simply be a school whose teachers teach well. On this evidence, the culture association is not teaching quality under another name.

Third, the schools in the top half on both warmth and strictness outperform the rest by two-fifths of a GCSE grade per subject for every pupil, while the interaction between the two dimensions is indistinguishable from
zero in the visited sample. In other words, the dimensions add to one
another rather than amplifying each other. The warmth that predicts progress is the warmth the observers saw, not the warmth the head described. Use the headteacher's own account of the school instead of the enacted scores and the association disappears.

The fourth result is the national extension. Through the previous chapter's text-derived scores, predicted warmth, predicted teaching and mark-scheme strictness each predict progress across the national population. The associations hold with the inspection grade held constant, and they hold on reports written before either outcome cohort's GCSE results existed, so they are neither the grade restated nor inspectors describing results they already knew.

The next section describes the data. The paper then sets out the
specification and what it can and cannot identify, reports the results and
their robustness, and concludes.

% ============================================================
\section{Data}
\label{sec:p2_data}

\textbf{Outcomes.} The outcome is Progress~8, which is the Department for
Education's school-level value-added measure. For each pupil, attainment
across eight GCSE slots, weighted towards English and Maths, is compared with
the national average attainment of pupils who left primary school with the
same Key Stage~2 result, and the differences are then averaged across the
school's cohort. A school at zero is exactly average for its intake. A score
of $+0.5$ means that its pupils achieved half a grade more per subject than
pupils elsewhere with the same starting point, and $-0.5$ means half a grade
less. Nationally the measure has a standard deviation across schools of
about $0.5$ and an interquartile range of about two-thirds of a grade. This
means that the difference between a school at the first quartile and one at
the third is roughly two-thirds of a grade in every subject for every pupil.

The outcome is a value-added measure, and value-added measures have their
critics. \citet{gorard2010,gorard2013} argue that school-level estimates are
dominated by error and unstable from year to year. I use Progress~8
nonetheless, for three reasons. It is the measure that the Department for
Education publishes and by which schools are judged; it conditions on the
single strongest predictor of GCSE performance, which is prior attainment at
Key Stage~2; and it allows the visited schools to be placed against every
school in the country on the same footing. I meet the concern about
instability by averaging two cohorts and by showing, in the appendix, that
a school's Progress~8 is stable across years (\cref{app:3A:stability})
relative to the differences between schools.

I report overall Progress~8 and its English and Maths components, which are
the cleanest measures the tables offer because every pupil sits both of
those examinations. The remaining components, EBacc and Open, are partly
determined by which subjects a school enters its pupils for, and they are
reported in \cref{app:3A:tables} together with a direct analysis of subject
entry rates; \cref{app:3A:p8mechanics} sets out how the components are
constructed.

The primary outcome is the two-year average of the 2022/23 and
2023/24 cohorts, which are the two post-pandemic years for which a Key
Stage~2 baseline exists. Averaging the two reduces the year-to-year noise
in a single cohort's figure, and it also dilutes any cohort-specific
disruption: the Department advised caution over the 2022/23 measures
because of the pandemic's effect on that cohort. The 2024/25 cohort has no baseline, because its
Key Stage~2 tests were cancelled in 2020, and \cref{app:3A:tables} reports a
pseudo-progress measure for it that correlates $+0.81$ with real
Progress~8.

\textbf{Culture measures.} The explanatory variables of interest are the enacted warmth and strictness scores of the previous chapter, measured on a 0--10 scale for the \NTierOne{} visited schools. They are
entered per standard deviation so that the two coefficients are comparable.
Across the visited schools enacted warmth has a mean of $6.4$ and a standard
deviation of $0.9$, and enacted strictness a mean of $7.0$ and a standard
deviation of $0.9$, so a standard deviation is about one point on the
ten-point scale.

Two properties of these scores, which were established in
the previous chapter, bear on how the estimates below should be read. The
first is that a school's score is an average over lessons that differ from
one another more than schools differ from each other. Within-school lesson
variation is about three times the between-school variation for in-lesson
strictness. This means that ``a strict school'' here is a school whose
lessons are orderly on average, and not one in which every lesson is. The
second is that the scores carry measurement error, with a reliability
of about 0.7. For a score entered on its own, error of this kind biases
the coefficient towards zero, so those estimates are conservative. Entered together, the two scores behave less simply. Warmth and strictness are correlated, so the regression's job is to decide how much of the joint association belongs to each. Noise interferes with exactly that decision: when one score is measured with more error than the other, some of its association is credited to the other score instead. So when the two scores enter together, the error makes the division between the two coefficients uncertain: part of the warmth association can appear as strictness, and the reverse. The error cannot produce the overall association. It moves estimates towards zero, so if anything the association between culture and progress is understated. About a third
of the observed lessons were rated by one researcher rather than two, and the
robustness section checks that the estimates do not depend on those schools.

The visit's teaching-practice score, built from the ten lesson items on questioning, feedback, explanation and structure, enters as the rival explanation to be tested. It
is the most obvious rival explanation for any association between culture
and progress, since a school whose teachers are warm and orderly may also be
a school whose teachers teach well. The espoused scores, from the headteacher
interview, enter one robustness specification.

Three text-derived scores
enter the national extension, all built and validated in the previous
chapter and all reading the school's most recent inspection report with
its stated grades removed. Warmth and teaching come from prediction
models: each learns, from the visited schools, which words and phrases of
a report go with higher enacted scores, and every national school's
prediction comes from a model that never saw it. Both models are trained
on the part of the enacted score that intake statistics cannot explain,
so they earn no credit for recognising demographics. Their held-out
correlations with the enacted scores are $\PredWOfsted$ for warmth and
$\PredTOfsted$ for teaching. Strictness is read differently: a one-page mark scheme applied once per report by a language model, frozen before any scoring. Its bands correlate with
enacted strictness at $\RubricSOfstedRaw$.

Four further website-derived measures enter an exploratory section only:
indicators of a traditional pedagogy and a traditional ethos, the school's
religious character, and the prominence of faith in its self-presentation.

The previous chapter explains why the two dimensions are read differently: warmth shows in a report's everyday wording, which counting can pick up, while strictness sits in specific claims that a reader has to be told to look for. \Cref{fig:national_bands}, reported with the national extension, shows the national distribution of the strictness bands.

\textbf{Controls.} All specifications condition on the same set of
controls. The first is the mean Key Stage~2 score of the cohort. Progress~8 already compares each pupil with pupils who left primary school at the same level, so intake should not matter to it, but the adjustment may be imperfect. The control protects the estimates if it is. The next are the shares of pupils eligible for free school meals, with English as an additional language (EAL) and with special educational needs, which are the
composition measures most strongly associated with progress. Then come log
school size; academy status, urban location and selective admissions; and
the number of years since the school's last inspection, since a school that
was inspected long ago is described by an older report. Finally, the
school's overall inspection grade as at August 2019 enters as indicators for grades below Good,
pooling Outstanding with Good. Outstanding schools were exempt from
routine inspection before 2020, so the Outstanding label carries the
oldest information in the variable. I use the 2019 grade rather than the current one because a
contemporaneous grade is partly an outcome of the culture being measured. I
enter it at all because it is the best available summary of how the school
was regarded before the outcome
cohorts arrived.

Seven of the 103 visited schools carry no 2019 grade under their current
URN, mostly through conversion to an academy after 2019. One of the seven
is excluded from the outcome regressions anyway as a late-entry school (schools admitting only from age 13; defined below),
and one lacks the years-since-inspection control, so five remain in the
estimation sample. The predecessor school's grade fills four of them, and
the fifth has no traceable predecessor and is dropped where the grade
control is required.

\textbf{Sample.} Three exclusions apply. The first is schools whose statutory
entry age is 13 or above (university technical colleges, studio schools and
similar; 98 schools nationally, two of them among those visited), which are
excluded from every outcome regression. Progress~8 measures progress from
age 11, and these schools enrol their pupils at 13 or 14, so their figure
attributes to them progress that was made elsewhere. The second is one
visited school that lacks the years-since-inspection control, and the
third is the school with no fillable 2019 grade, described above. The
primary estimation sample is therefore $n = \PrimarySpecN$: 103 schools,
less the two late-entry schools, one missing control and one unfillable
grade.

\Cref{tab:outcome_stats}
and \cref{tab:control_stats} report descriptive statistics for the outcomes
and the controls. The visited schools' mean Progress~8 is about a quarter of
a grade above zero, with a standard deviation a little below the national
one, which is the restricted range noted in the previous chapter. The visited schools are stronger on average than the national population, so the estimates apply most directly to a population somewhat above the
national average, and the restriction of both culture and outcomes works
against finding associations rather than for them. The
interviewed schools are the previous chapter's 303, of which the
visited are a subset; the descriptive tables show both groups. On the
controls the visited schools are close to the interviewed ones and to the
country as a whole: 29 per cent of pupils eligible for free school meals, 16
per cent with special educational needs, three-quarters academies, nine in
ten urban, one in twenty selective, and a 2019 grade distribution of roughly
a quarter Outstanding, five-eighths Good and one in nine below Good. The one
visible difference is the share of pupils with English as an additional
language: 28 per cent, against 21 per cent in the interviewed sample. The
difference reflects the London weighting, and the share is controlled for
throughout. The correlations between the culture scores and the controls, reported in \cref{tab:score_controls_corr}, are all below $|0.3|$. This means that the culture measures are not close proxies for intake or
school type, and that the coefficients below cannot simply be reflecting
differences in pupil composition.

\input{tables/tab_outcome_stats}

\input{tables/tab_control_stats}

% ============================================================
\section{Empirical Specification}
\label{sec:p2_spec}

The primary specification is
%
\begin{equation}
  \label{eq:main}
  Y_s = \alpha + \beta_W W_s + \beta_S S_s + \mathbf{X}_s'\gamma
        + \sum_g \delta_g \,\mathbb{1}[G_s = g] + \varepsilon_s,
\end{equation}
%
where $Y_s$ is the two-year average Progress~8 (overall, English or Maths)
of school $s$, $W_s$ and $S_s$ are its enacted warmth and strictness scores
in standard-deviation units, $\mathbf{X}_s$ is the set of controls listed
above and $G_s$ is its 2019 inspection grade. Standard errors are
heteroskedasticity-consistent (HC3), which is the small-sample variant that
is appropriate when $n$ is near 100. I estimate three stages:
equation~(\ref{eq:main}); the same equation with the teaching-practice score
$T_s$ in place of $W_s$ and $S_s$; and the equation with all three. Each
dimension is also entered on its own. The national extension replaces $W_s$
and $S_s$ with the text-derived scores of the previous chapter:
predicted warmth, predicted teaching, and the mark-scheme strictness
bands, each standardised and entered on its own, on all schools with an
outcome, estimated both with and without the grade dummies.

The estimates are associations. What the design does and does not achieve can be stated exactly. Because the outcome is value-added, the
comparison is between schools whose pupils started from the same place. This
means that the first and largest confound in any school-effects estimate,
namely that stronger schools take stronger pupils, is removed by
construction, and the Key Stage~2 control guards against what remains.
Half of the visits took place during 2023/24, the second outcome cohort's
own year, and half in the year after it. The timing therefore does not
rule out ordinary reverse causation: good results may produce a warm and
orderly school.

What the design relies on instead is that culture is persistent, so that
what was observed in 2023/24 and 2024/25 is a reasonable measure of what
the 2022/23 and 2023/24 cohorts experienced. This is an assumption, and it is not tested directly. No school was visited twice, so the persistence of culture itself is never measured. What can be shown is circumstantial, and three pieces of it point the same way.

The first is stability. A school's
Progress~8, and the structural characteristics that shape it, are stable
across years relative to the differences between schools, which is consistent with stable culture without establishing it. The second is the contemporaneous cohort. The 2024/25 cohort, the one measured at the same time as the fieldwork, gives coefficients close to the primary ones on its pseudo-progress measure. That check is reported with the robustness results. The third comes from the national extension: old inspection reports predict progress almost as well as recent ones. Some schools were last inspected many years ago, so their reports describe an older version of the school; if culture drifted quickly, those reports should predict today's progress much worse than fresh ones do. They predict only slightly worse: each additional year since a report was written weakens the association between strictness and progress by about $0.003$ ($p = 0.09$). The fade is real, and I read it as such, but it is slow.

Two features of the design narrow the risk further. Because the 2019 grade is held constant, the comparison is between
schools that the inspectorate regarded alike before the outcome cohorts
arrived. A school's reputation is therefore partly absorbed, together with
whatever staff and families that reputation draws in. Finally, because the sample excludes schools that enrol at 13 or 14, no
school is credited with progress that was made at another school.

What the design cannot do is rule out the possibility that some persistent
characteristic of a school produces both its culture and its results. This
might be a headteacher who is effective in ways that the visit does not
measure, or a stable school reputation that draws in both committed staff
and motivated families. Two features of the data bear on this without
resolving it. The teaching-practice score is observed alongside the culture
scores, so the most obvious such characteristic, that warm and orderly
schools are simply schools whose teachers teach well, can be tested directly
rather than assumed away. The second feature is that each association appears again across the
national population using a text-derived score with the inspection grade
held constant, so it is not an artefact of which schools agreed to be
visited. One sorting concern remains: strict schools may
retain fewer pupils with social, emotional and mental-health needs and gain
in measured progress by that route. A measure of that share taken before the outcome years tests it, in the robustness section.

% ============================================================
\section{Results}
\label{sec:p2_results}

\textbf{Culture is associated with progress.} \Cref{tab:spec_ladder} reports the
primary estimates. In the primary specification enacted warmth has a
coefficient of $\hat{\beta}_W = \PrimarySpecW$ ($p = 0.001$) and enacted strictness a coefficient of $\hat{\beta}_S = \PrimarySpecS$ ($p = 0.012$) on
overall Progress~8, with $R^2 = 0.62$. Warmth is the larger of the two
coefficients, although the difference between them is not itself
significant ($p = \WaldWSP$). The table shows the same equation
under four treatments of the grade control, from requiring an original 2019
grade ($n = 95$) to dropping the control altogether ($n = 100$), and the
coefficients move by no more than $0.02$ across the four. Both dimensions
contribute, as the authoritative-school framework predicts.

\input{tables/tab_spec_ladder}

These magnitudes can be put in context. A standard deviation of
enacted warmth is about one point on the ten-point scale, or half a point on
the five-point scale of every warmth item in every lesson observed. The
associated $0.15$ of a grade is somewhat under a third of a standard
deviation of school Progress~8 nationally. It is also somewhat over a quarter of the gap in mean
Progress~8 between schools graded Outstanding and Good in 2019, a gap of
$\GapOutstandingGood$, and about a quarter of the gap between the least
and the most disadvantaged quarter of schools by free-school-meal share, a
gap of $\GapFsmQuartiles$. Two standard deviations on both dimensions together, which is roughly the
distance from a school near the fifteenth percentile on both dimensions to
one near the eighty-fifth, is associated with about half a grade per subject per
pupil. That is the size
of the difference that the performance tables treat as separating a
well-above-average school from an average one.

Each dimension, entered on its own, has a coefficient of about $0.2$: \cref{tab:univariate_ws} reports $0.219$ for warmth and $0.199$ for strictness.
When the two enter jointly, warmth keeps two-thirds of its solo coefficient
and strictness about half. The two scores overlap, as their correlation of $\CorrWarmthStrictnessEnacted$ implies, but each keeps a substantial association of its own. If there were a single scale running from warm to strict, one coefficient would absorb the other. That correlation, though, partly reflects how the scores were made: the same observers scored both dimensions at the same school on the same day, which pushes the two scores together. The previous chapter's estimate of the correlation between the dimensions themselves, with that pushed-together part stripped out, is about \CorrWSTrue{}, so the case for treating them as two separate things is stronger still.

\input{tables/tab_univariate_ws}

The controls play different roles for the two dimensions. With no controls at all, warmth has a coefficient of
\SensShortW{} on the same schools ($p = \SensShortWP$) and strictness
\SensShortS{} ($p = \SensShortSP$): unconditionally, the strict
schools show the clearer progress advantage. Adding the share of
pupils with English as an additional language, and nothing else, moves the
warmth coefficient to \SensShortEalW{} and the strictness coefficient to
\SensShortEalS{}, which is close to the full-specification split
(the no-control estimates are tabulated with the sensitivity analysis, \cref{tab:sensitivity_bounds}). The
reason is that schools with many EAL pupils make more progress on average
and are scored somewhat lower on enacted warmth, so a comparison that
ignores intake composition hides the warmth association. The warmth result
is therefore a conditional one. It appears when schools are compared with
others of similar intake, which is the comparison the specification is built to make. This conditionality matters when reading the sensitivity analysis below. The lower warmth
scores of high-EAL schools could partly reflect how observers read warmth
in different contexts rather than the schools themselves. The regression cannot tell these two cases apart. If the scores are right, the adjusted warmth association is a fact about the schools. If the observers read warmth differently in different contexts, part of it is a correction for how they scored.

At the level of the components, warmth is significant for both subjects
(English $0.162$, $p < 0.001$; Maths $0.135$, $p = 0.011$), while
strictness falls short of significance in each taken alone (English
$0.071$, $p = 0.11$; Maths $0.076$, $p = 0.09$). Overall Progress~8 is not
the sum of these two components: it also counts the EBacc and Open slots,
and it is there that the strictness association is clearest (EBacc $0.138$,
$p = 0.008$; Open $0.119$, $p = 0.035$). Those two components mix progress with the school's entry decisions, so part of the overall strictness association could come from stricter schools entering pupils for different subjects rather than from their pupils progressing more. Testing that route means first showing that stricter schools do enter pupils differently, and the visited sample is too small to show it: there, strictness does not significantly predict EBacc entry. The national sample introduced below is large enough, and the entry analysis reported with the national extension finds the route to be small. The full component
estimates and the entry analysis are in \cref{app:3A:tables}.

\textbf{Teaching quality does not account for it.} \Cref{tab:stages23_trio}
reports the second and third stages. On its own, the visit's
teaching-practice score predicts overall Progress~8 at $0.206$ ($p <
0.001$), but with a lower $R^2$ ($0.55$) than the two culture scores. When it
is added to the primary specification it enters at $0.015$ ($p = 0.82$), and
the culture coefficients are essentially unchanged ($0.137$ and $0.103$).

There are two possible readings of this result, and the data do not allow
a choice between them. The first reading is that culture and instruction
are distinct predictors of progress. On this reading, what a school is like
between and around lessons, and how orderly its lessons are, matter over and
above how well the lessons are taught, and the culture association is not
simply the teaching-quality association under another name. The second
reading is that the teaching score's signal is absorbed by the two culture
scores with which it is highly correlated. The teaching items are the ones
on which the observers agree least lesson by lesson. However, once
averaged to a school score the teaching measure is about as reliable as
the culture scores: $0.67$, against $0.60$ and $0.58$ for the in-lesson
components. This reading therefore rests on the collinearity rather than
on the teaching score being markedly noisier. On this reading the null result on teaching
understates what instruction contributes. Two facts favour the first
reading, although they do not settle the matter. Firstly, the teaching
score on its own predicts progress well, so it is not just noise.
Secondly, the culture association runs partly through out-of-lesson
warmth (shown below), which no measure of classroom teaching could pick up.
What would separate the two readings is a measure of teaching quality that
is independent of the visit, and no such measure exists for these schools.
The claim I make is therefore the narrower one, which is that conditioning
on enacted teaching quality does not account for the culture association.

\input{tables/tab_stages23_trio}

\textbf{The dimensions add.} \Cref{fig:quadrant_ws} plots the visited
schools by their two culture scores, shaded by progress. Schools in the warm
and strict quadrant are predominantly above-average performers, and the
other three quadrants are all populated, which is what makes it possible to
test the combination. Formally, the warm-and-strict quadrant sits $0.40$ of
a grade above the rest of the sample after controls ($p < 0.001$), while the
interaction between the two standardised scores is zero ($0.039$, $p =
0.27$; \cref{tab:typology}). In other words, warmth and strictness
add to one another rather than amplifying each other. The schools with the most progress have both because each of them helps. The data show no combination effect, and a
school that is strong on one dimension still gains from the other.
The national extension below repeats this test at thirty times the sample and reaches the same additive conclusion.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.85\textwidth]{fig_quadrant_ws.pdf}
  \caption{Observed warmth and strictness for the visited schools, shaded by
  Progress~8 (two-year average). Dashed lines mark sample medians. The
  adjusted quadrant comparison is in \cref{tab:typology}.}
  \label{fig:quadrant_ws}
\end{figure}

\textbf{Where the association sits within the scores.} \Cref{tab:subscores} breaks the enacted scores into their five blocks: warmth in lessons, warmth outside them, strictness in lessons, strictness outside them, and teaching. Item-level associations, corrected for multiple testing, are in \cref{tab:items_fdr}. Entered together, two of the five keep their associations: warmth outside lessons ($0.111$, $p = 0.006$) and strictness inside them ($0.115$, $p = 0.008$). Their counterparts lose their associations. Read literally, the relational quality that goes with progress is the one visible in corridors and at break, and the order that goes with it is the one visible in lessons. The five blocks overlap heavily, though. When measures overlap, small amounts of noise decide which of them the regression credits, so the ranking is fragile at this sample size and the literal reading should be held loosely.

\textbf{Espoused warmth does not predict progress.} Substituting the
headteacher's espoused scores for the enacted ones on the same schools
reduces the warmth coefficient to $0.051$ ($p = 0.29$), while espoused
strictness retains an association of the same size as the enacted one
($0.120$, $p = 0.018$); \cref{tab:enacted_espoused} reports both regressions. So far as this sample can tell, what a headteacher says about warmth
predicts progress no better than chance. What they say about behaviour
systems carries some signal. This is the pattern that the previous
chapter's measurement predicts, since espoused warmth was shown there to be
only weakly related to enacted warmth, and espoused strictness more
closely related to enacted strictness. Part of the warmth result may reflect the shape of the espoused scale rather than a pure absence of information. Heads overstate warmth against the enacted scores by \GapWarmth{} points on average, against \GapStrictness{} for strictness, so the espoused warmth scores bunch near the top of their scale, and scores that bunch have little variation left to predict with. The result is also a check on the
enacted measures as much as on the espoused ones. Had the espoused scores predicted progress as well as
the enacted ones, the visits would have added nothing that an hour with the
headteacher could not supply. Two related results are in
\cref{app:3A:tables}. A second question is whether the size of a head's overstatement matters in itself: whether a school whose head claims much more than the observers saw does worse, or better, than one whose head claims only what they saw. Once the enacted score is in the regression, the gap between the espoused and enacted scores predicts nothing. And the pattern is not confined to what schools say about themselves: on the national sample introduced below, parents' survey responses on their children's happiness and safety are positively associated with progress, at $0.15$ per standard deviation. Satisfaction and results are jointly determined, though, so this is corroboration rather than an independent test.

\input{tables/tab_enacted_espoused}

\textbf{National extension.} The estimates so far rest on a hundred schools that agreed to be visited. The national test asks whether the associations survive outside that sample, on schools that no one visited. \Cref{tab:national_legs} reports these regressions. For \NNationalStrictness{}
schools with an outcome, some thirty times the visited sample,%
\footnote{The national sample starts from the 3,332 schools of the national population: 60 schools have no scored inspection report, mostly because they opened too recently to have been inspected; a further 80 have no published Progress~8, mostly because their first cohort has not yet reached GCSEs; 95 are excluded as late-entry schools, the same exclusion the visited sample applies; and 45 lack a control variable. That leaves 3,052. \Cref{app:3A:robust} checks whether these omissions could bias the estimates.} warmth
and teaching are predicted from each school's inspection report by the
models of the previous chapter, and strictness is read by the one-page
mark scheme. Every warmth and teaching prediction comes from a model
that never saw the school. All three coefficients are positive and precise:
$\NatW$ per standard deviation for warmth, $\NatT$ for teaching and
$\NatS$ for strictness, each with $p < 0.001$. The three coefficients cannot be ranked against one another. Each is the association for a different score, and the three scores are not equally close readings of their dimensions: each captures a different share of its dimension, and each picks up a different amount of everything else a report says about a school. A larger coefficient can therefore mean a stronger association, or just a score that carries more of the report. The scores' weakness cannot create these associations. Each score captures only part of its dimension, and the part it misses behaves like noise in the regression. Noise pulls a coefficient towards zero, not away from it, so if the scores captured culture in full, the coefficients would if anything be larger. The imperfection raises a different concern. Besides culture, each score picks up other things a report says about a school, and those other things might predict progress on their own. The grade and timing checks below are aimed at exactly that possibility. Checks on the three estimates come first; what else the national data show follows.

\input{tables/tab_national_legs}

Four checks constrain these estimates. The first concerns the inspection grade.
A score read from a report might merely restate the inspectorate's
grade, in which case its association would be the grade's association.
Adding the 2019 grade dummies moves the coefficients almost nowhere
(warmth $\NatWGrade$, teaching $\NatTGrade$, strictness $\NatSGrade$): what the text says predicts
progress over and above what the grade says. The second concerns timing. On reports
published before September 2022, before either outcome cohort's results
existed, the coefficients are $\NatWPre$, $\NatTPre$
and $\NatSPre$, essentially their full-sample values, so reports that
could have been written in knowledge of the outcomes are not driving the
associations. The third concerns the models' own estimation error. The warmth and teaching scores are themselves the output of a fitted model, so they carry that model's uncertainty as well as the regression's. Refitting the model many times on resampled training schools and rerunning the regression each time gives 95 per cent intervals of [$\NatWBootLo$, $\NatWBootHi$] for warmth and [$\NatTBootLo$, $\NatTBootHi$] for teaching, both away from zero. The fourth concerns sample dependence: excluding the visited schools from the national sample raises the warmth coefficient (to $+0.136$) and the teaching coefficient (to $+0.122$), so neither association is an artefact of the schools that agreed to take part.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.7\textwidth]{fig_ch2_bands.pdf}
  \caption{The national distribution of strictness bands under the
  one-page mark scheme (3,324 schools with a scored report; the regressions run on the 3,052 with an outcome and full controls). All five bands are used;
  band 4, conduct presented as a strength, is the national mode.}
  \label{fig:national_bands}
\end{figure}

Each national coefficient sits roughly where the quality of its score predicts, with one exception. A score that captures only part of its dimension should show a correspondingly scaled-down coefficient, and the warmth and teaching estimates sit close to that expectation. Strictness sits above it. The mark scheme reads the report as it stands, so its bands carry some of the report's general character as well as its strictness, and that general character is itself associated with results. For the ranking of the two dimensions, the visited sample is the one to trust, because there the dimensions were measured directly. \Cref{app:3A:benchmarks} works through the benchmark for each coefficient.

The scheme's openness admits one further worry. Inspectors know a school's exam results when they write, and a school with good results may receive a more favourable account of its conduct than its corridors alone would justify. If that happens, the strictness score partly measures results, and part of the strictness coefficient is results predicting results. The pre-2022 check rules out the direct version: reports written before the outcome years still show the association, and those inspectors could not have known the outcomes. It cannot rule out the indirect version. An inspector who saw the school's earlier results may still have written a more favourable account of conduct, and earlier results resemble current ones, so some of the association may still run through results rather than strictness.

The national warmth estimate carries one further, independent corroboration from
the previous chapter: on the national schools outside training, the same
predicted warmth correlates $\PVPredWarmth$ with parents' reports of
happy, safe children. A text-derived score that tracks both pupil progress and parent experience, on schools it never saw, is unlikely to be an artefact of any one source.

The combination result also repeats at this scale. Crossing predicted warmth with the strictness bands, the interaction is indistinguishable from zero ($0.004$, $p = 0.56$), while the schools high on both dimensions sit $0.36$ of a grade above the rest after controls, or $0.26$ with the inspection grade held constant (\cref{tab:typology}). The national split is asymmetric, though: the band median falls at four, so the high-strictness half is in effect the top band, and the low-warmth, high-strictness cell is small.

A further question is whether the strictness association runs through the curriculum rather than through progress within it. Stricter schools' pupils are more often entered for the academic pathway. However, controlling for EBacc entry removes only 10 to 14~per~cent of the association nationally, and about 11~per~cent in the visited sample, so most of the association reflects progress within subjects. \Cref{app:3A:tables} reports the entry regressions.

What the extension still does not provide
is identification: a text-derived score is open to the same omitted-variable concerns as an enacted one, and the sensitivity analysis below remains the measure of how strong a confounder would have to be to overturn the visited estimates. Consistent with the
previous chapter's measurement findings, the schools' own documents
supply no rival signal: behaviour-policy and website scores are not
positively associated with progress in any specification
(\cref{tab:llm_p8_matrix}).

\textbf{Robustness.} \Cref{tab:robustness_overall} puts the primary estimate through eight 
checks, each aimed at a particular doubt: that the grade control is doing the work; that a 
result found in a two-year average fails in a single year; that strict schools shed pupils 
with social, emotional and mental-health needs and gain measured progress that way; that the 
result is a property of Progress~8 rather than of attainment; that it depends on the lessons rated by one researcher rather than two; that it is a London artefact; or that it depends on a single school. \Cref{app:3A:robust} walks through the checks one by one. Every check but two 
leaves both coefficients significant at the five per cent level or better. The exceptions are both for strictness: the Attainment~8 English component, and the subset of schools where every lesson was double-rated. Nationally, clustering the standard errors on academy trust moves them by at most ten per cent (\cref{app:3A:robust}).

\input{tables/tab_robustness_overall}

Two further checks concern timing and the headteacher. Within-school
Progress~8 is stable across years: the mean within-school standard
deviation is $0.20$, against a between-school standard deviation of about
$0.5$ (\cref{tab:stability_p8}). This stability supports using culture
measured after the outcome window as a proxy for culture during it. The
2024/25 pseudo-progress measure is the one cohort measured at the same
time as the fieldwork. It gives coefficients within $0.04$ of their
real-Progress~8 counterparts on the overall measure (\cref{tab:p8_proxy}),
and the contemporaneous strictness coefficient is if anything larger. That observation is consistent with the timing argument, but a same-year association is equally open to results shaping the school's orderliness, so it is not counted as evidence on its own.

A placebo outcome sharpens the timing question. If culture is a persistent trait of a school, the culture observed in 2023--25 should relate even to results from long before; if the association belongs to the recent years alone, it should not. The test regresses the school's pre-pandemic Progress~8, from 2018/19, on the culture scores observed in 2023--25, with that era's controls where they exist. The old results are related, but more weakly: $+0.09$ for warmth and $+0.08$ for strictness on the 93 schools with both outcomes, against $+0.16$ and $+0.10$ for current results on the same schools, and neither placebo coefficient is significant.

The informative comparison is whether today's culture fits today's results better than it fits the past's. The point estimates say it does, most visibly for warmth, where the old association is about six-tenths of the current one. A formal test, though, cannot reject that the old and current associations are equal ($p = 0.17$ for warmth and $0.62$ for strictness with era controls, $0.09$ and $0.57$ with current ones). The placebo points in the direction the timing argument predicts without settling it.

A stricter version of the timing check keeps only the 22 schools whose headteacher, named in the most recent inspection report, is confirmed still in post in July 2026; there the culture observed is most plausibly the culture the outcome cohorts had. Its standard errors are three to seven times the full-sample ones, so the subsample can support no conclusion in either direction. \Cref{tab:timing_checks} collects the placebo and continuity estimates with their standard errors.

\input{tables/tab_timing_checks}

Finally, two standard measures ask how strong an unmeasured confounder would have to be to overturn these estimates (\cref{tab:sensitivity_bounds}). By the simpler of the two, the E-value, a single confounder would need an association of at least \EValW{} in risk-ratio terms with both warmth and progress, and \EValS{} for strictness, to account for the estimates on its own. The other, Oster's $\delta$, reaches a similar verdict for strictness and a more favourable one for warmth, because the controls raise the warmth coefficient rather than lower it, and a measure built on extrapolating the controls' behaviour rewards that pattern; \cref{app:3A:robust} sets out both measures in full. Neither measure touches reverse causation, and both are likelier understatements, because measurement error biases the coefficients they start from towards zero.

\textbf{Exploratory: what else websites reveal.} The same public record carries identity signals beyond culture. The previous chapter's identity measures, read from school
websites and, for faith prominence, validated against the official
register, carry their own associations with progress. \Cref{tab:identity} reports them.

\input{tables/tab_identity}

Schools whose websites espouse a traditional
pedagogy make more progress ($\IdPed$ of a grade per subject,
$p < 0.01$), as do
schools with a traditional ethos ($\IdEth$), and both survive the grade
control. Prominence of faith shows a modest gradient: schools where faith
is central to the site's self-presentation sit $\IdFaithCentral$ above
otherwise similar non-faith schools, an estimate that weakens with the
grade control. These results are exploratory. The measures come from what schools choose to say about themselves, and the regressions were designed after the data had been seen, so a significance level here cannot carry the weight it carries elsewhere in the chapter. The associations are therefore reported as descriptions rather than tests.

% ============================================================
\section{Conclusion}
\label{sec:p2_conclusion}

Observed school culture is associated with pupil progress. Across the
\NTierOne{}
English secondary schools that were visited, a standard deviation of
enacted warmth is associated with Progress~8 that is $0.15$ of a grade
higher, and a standard deviation of enacted strictness with $0.11$ of a
grade, and both coefficients are stable across every treatment of the
inspection-grade control. The two associations differ in how they behave rather than in size, and the difference between the coefficients is not significant. The warmth association appears only when schools are compared with others of similar intake. Once intake is held constant, an unmeasured confounder would need to be stronger to explain the warmth association away than the strictness one. The strictness association is present before any controls are added, but it is the more exposed to such a confounder. 

The two dimensions contribute additively, and the schools that combine them outperform the rest by two-fifths of a grade. Conditioning on enacted teaching quality
does not account for the association. The headteacher's own account of the
school predicts nothing detectable on warmth. And the associations are not a property of the hundred schools that agreed to be visited: all three scores predict progress across the national population, with warmth and teaching read from inspection reports by the prediction models and strictness by the one-page mark scheme. Each holds with the inspection grade held constant, and on reports that predate the outcome years.

Against the literature the paper sits in, the results say three things. To the school-effects tradition, they put numbers on two of the features of ethos that Rutter and the effective-schools syntheses named, an orderly environment and a supportive climate. The reading comes from direct observation in a hundred ordinary schools rather than a case-study sample, and it extends to the national population through the text-derived scores. To the charter and academy literature, they say that the two halves of the package, structure and support, each matter when they are measured apart, and that neither is the whole of the effect.
To the authoritative-school literature, they supply the
academic-progress outcome that it has mostly lacked, and they find what the
typology predicts: both dimensions matter, and the schools with both do best.

Two implications bear on the current policy debate. Both are conditional on the association being at least partly causal, which the design supports but does not establish.

The first concerns the behaviour debate. That debate treats discipline as what improves schools, and relational culture as secondary. The estimates here do not rank the two that way. Warmth matters about as much as strictness, and the two add rather than multiply. An improvement drive that tightens behaviour while letting relationships slip therefore gains on one dimension, loses about as much on the other, and ends roughly where it began. The schools two-fifths of a grade ahead are the ones with both. The quadrant groups are small, so this reads from the coefficients rather than from comparing the groups directly.

The second concerns where the relational work matters. The association runs through warmth outside lessons and order inside them. The warmth that goes with progress is not classroom technique but how adults and pupils deal with each other in corridors, at break and at the gate.

The estimates are associations. They are strengthened by a value-added
outcome, by the persistence of school characteristics across years, by a
rich set of controls and by their
stability across specifications, but they are not causal estimates: a persistent characteristic that produces both a school's culture and its results cannot be ruled out with cross-sectional data. The visited sample is small and stronger than the national average, culture
was measured during or after the final outcome year, and a single visit measures a school with considerable error.

A longitudinal design, with culture observed before the outcome cohort arrived and again afterwards,
would provide evidence on the direction of the association, and a design
that enacted culture changing within schools, as headteachers changed or
behaviour systems were introduced, would provide evidence on its cause.

Within the present limits, the results pick out the relational and disciplinary features of a school that go together with its pupils' progress. They also show that both features, and teaching besides, can be read at national scale from the public record: warmth and teaching by a model that counts words, and strictness by a mark scheme that tells the reader what to look for.

% ===============================================================
% APPENDIX 3A
% ===============================================================
\clearpage
\startchapterappendix{3}
\begin{center}
  {\LARGE\bfseries Appendix to Chapter 3}\\[6pt]
  \rule{0.5\linewidth}{0.4pt}
\end{center}
\medskip
\noindent This appendix collects the supporting material for the analyses
reported in the chapter. It explains how the Progress~8 measures and the subject entry rates are calculated. It sets out the evidence on the stability of school characteristics that underpins the choice of outcome years, and gives
the full five-outcome versions of the main results tables (the chapter body
reports overall, English and Maths Progress~8, and the EBacc and Open
components appear here) together with the secondary and exploratory
analyses.
\medskip

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\section{How the Progress 8 Measures Are Calculated}
\label{app:3A:p8mechanics}

The eight slots of Progress~8 (P8 below) are structured, and this
structure is why the thesis treats the components differently. The measure
itself is as the body defines it: attainment across eight GCSE slots,
compared with the national average for pupils with the same Key Stage~2
starting point, and averaged across the school. English and Maths each fill two slots and
are double-weighted. Every pupil sits both, so these components measure
progress and nothing else. The EBacc component covers three slots that must be filled
from a restricted academic list: sciences, computer science, history,
geography, and modern foreign languages. The Open component covers the final
three slots, which may be filled by any approved qualification, including
arts and vocational subjects. For these last six slots the school makes
entry decisions, and an unfilled slot contributes zero progress to the
school's average. Consequently, a school's EBacc and Open components mix two
different things: how much progress pupils make in the subjects they enter,
and which subjects the school enters them for. A school that enters few
pupils for languages will carry empty EBacc slots and a depressed EBacc
component even if the pupils it does enter progress well.

This structure generates a theoretical prediction about warmth and
strictness. Schools higher on strictness, characterised by structured
behaviour management, high academic expectations, and intensive pupil
steering, are more likely to direct pupils into the EBacc pathway, and could therefore raise their EBacc component through entry as well as through progress itself. Schools higher on warmth might generate stronger
progress in the more elective Open component. The five-outcome versions of the main results tables in this appendix
provide evidence on that prediction, and the entry-rate analysis below
addresses the pathway channel directly.

The entry rates themselves are calculated from the pupil count columns of
the 2023--24 performance tables, because the Department for Education
stopped publishing entry rates in percentage form in recent releases. The
EBacc entry rate is the number of pupils entered for the full EBacc suite
divided by the number of pupils at the end of Key Stage~4, and the
humanities and languages rates are constructed in the same way from their
respective counts. Entry rates are available only at the subject-group
level. Individual subjects within a group cannot be distinguished in the
data collected for this project.

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\section{Structural Stability of School Characteristics}
\label{app:3A:stability}

The intra-class correlation (ICC) decomposes total variance in a panel variable
into a between-school component and a within-school over-time component. An ICC
close to one indicates that schools are much more different from each other
than they are from their own past selves. In other words, year-on-year
fluctuation within a school is small relative to the persistent differences
between schools. ICC is estimated by
one-way random-effects ANOVA on the school-year panel, retaining only schools
with at least three years of non-missing data for the variable in question.

\Cref{tab:structural_stability} reports ICC estimates and mean absolute
year-on-year changes for nine panel variables covering pupil intake composition,
special educational needs prevalence, prior attainment, pupil progress, and
per-pupil expenditure. Six of nine variables exceed ICC~$= 0.69$, and all
exceed~$0.49$. Intake characteristics (the share of pupils with English as
an Additional Language, school roll, and the mean KS2 prior attainment of the
entering cohort) are especially stable, with ICC values between 0.81 and 0.92
and small mean absolute year-on-year changes relative to their between-school
spread.
Special educational needs prevalence (EHC plan share, ICC~$= 0.67$; SEN support
share, ICC~$= 0.65$) and school-level Progress~8 (ICC~$= 0.73$) show moderate to
high stability. Per-pupil expenditure, at ICC~$= 0.52$, is the most volatile, which
reflects its sensitivity to changes in national funding policy. Even so, the
majority of the variation in expenditure is between schools rather than
within schools over time.

The ICC evidence quantifies the stability that the design assumes: schools
occupy relatively persistent positions in the distribution of structural
characteristics, which supports using culture scores measured in 2023--24
and 2024--25 as proxies for the organisational conditions present during
the outcome period.

The same analysis at the level of the thirteen primary-need categories
within the SEN population shows moderate stability for the Social, Emotional
and Mental Health (SEMH) category (ICC~$= 0.545$), which supports the
baseline SEMH share entering as an additional control in the robustness
specification of \cref{tab:robustness_overall}. The lower stability of SEMH
relative to the aggregate SEN support share (ICC~$= 0.65$) is consistent
with SEMH composition being partly responsive to school behaviour and
exclusion practices, rather than being determined solely by fixed intake
characteristics.

\input{tables/tab_structural_stability}

\Cref{tab:p8_component_stability} reports ICC estimates separately for the four
Progress~8 components reported in this chapter.
Among the Progress~8 components,
stability varies from ICC~$= 0.587$ for the Open component to ICC~$= 0.748$
for EBacc, with English and Maths sitting between these at ICC~$\approx 0.67$.
EBacc is more stable than Open because curriculum-pathway decisions, meaning which subjects pupils are entered for, are governed by stable school policy and change little from year to year,
while the more varied Open component (arts, vocational and additional GCSEs) is subject to greater annual variation in subject mix. All four P8 components
exceed the per-pupil expenditure ICC of~$0.52$, which is the weakest result
in the main stability table. The cross-sectional design is therefore at
least as defensible for each component individually as for the structural
characteristics already discussed.

The Open component's lower stability means that the warmth coefficient is
estimated against a noisier outcome. The chapter's differential
prediction, set out in \cref{app:3A:p8mechanics}, expects warmth to show
most strongly in the Open component and strictness in the EBacc component.
With Open at ICC~$= 0.587$ against $0.748$ for EBacc, the warmth side of that prediction is tested against more noise and may be understated. Any
differential finding between components must be read against this
difference in outcome noise.

\input{tables/tab_p8_component_stability}

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\clearpage
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\section{Robustness in Detail}
\label{app:3A:robust}

This section walks through the checks summarised with the results: the alternative specifications of \cref{tab:robustness_overall}, the headteacher continuity split, the two sensitivity measures of \cref{tab:sensitivity_bounds}, the clustering of the national standard errors, and a check on the schools the national sample omits.

\Cref{tab:robustness_overall} reports the primary
estimate under alternative specifications, all on the primary sample, and
each of them addresses a particular doubt. The grade control might be doing
the work, or absorbing part of it. The specification ladder
(\cref{tab:spec_ladder}) already varies its treatment. Dropping the control
altogether leaves $0.153$ and $0.097$ ($n = 100$).

Averaging two cohorts might smooth over a result that holds in
only one of them. The single 2023/24 outcome year gives $0.155$ and $0.105$.

Strict schools might have fewer pupils with social, emotional and
mental-health needs, and gain in measured progress by that route. Adding the
2015/16 baseline share of such pupils, which was measured before any of the
culture was observed, gives $0.149$ and $0.090$ ($n = 94$), and the sorting
channel accounts for under a tenth of the strictness association
(\cref{tab:semh_mechanism}).

Progress~8 might be peculiar. On the 2024/25 cohort's Attainment~8, which does not condition on intake, both dimensions remain significant at the 5 per cent level on the total, at $1.40$ and $1.32$ points per standard deviation of the score. Attainment~8 sums grades over ten weighted subject slots, so these coefficients are roughly a seventh of a grade per subject. On the English component alone warmth remains significant ($0.35$) while strictness does not ($0.16$, $p = 0.12$). The Key Stage~2 control in
these regressions is the school-level mean for the Progress~8 cohorts,
since the 2024/25 cohort has no Key Stage~2 results of its own.

The enacted scores might reflect something about the visits themselves. About
a third of the observed lessons were rated by one researcher rather than
two, those lessons were scored a little lower, and the share of such lessons
in a school's visit correlates $-0.25$ with its warmth score. Adding that
share as a control moves nothing ($0.150$ and $0.105$; the share itself
has a coefficient of $0.025$, $p = 0.76$).

The warmth result might be a London artefact, since
the sample over-represents London and the EAL share, through which the
warmth association works, is concentrated there. Adding a London indicator built from the schools register moves warmth only to $0.152$ ($p = 0.001$) and strictness to $0.096$, and the indicator itself is not significant ($0.113$, $p = 0.33$), so the association is not a London artefact.

Restricting the sample to the 64 schools in which every lesson was rated by at least two researchers gives $0.189$ for warmth
($p = 0.001$) and $0.071$ for strictness ($p = 0.15$). The warmth estimate is
larger on that subset and the strictness estimate smaller, with a standard
error a quarter larger on two-thirds of the sample, so the strictness result
is the one that depends on the full sample.

No single school carries the visited estimates: dropping each school in turn moves the warmth coefficient by at most $0.019$ and the strictness coefficient by at most $0.017$, and no omission moves either across conventional significance.

In the headteacher continuity split reported with the timing checks, the strictness point estimates stay at or above the full-sample values and the warmth point estimate falls close to zero, but the standard errors are three to seven times the full-sample ones, so neither movement is informative.

The two sensitivity measures reason differently. The first, Oster's $\delta$, reasons from the controls that can be seen. Adding the full control set moves each coefficient by a known amount, and the measure extrapolates: if unobserved confounders behaved like the observed controls, how much of them it would take to drive the coefficient to zero. For warmth the question has a twist. Adding the controls raises the warmth coefficient rather than lowering it, and when the controls push a coefficient up, an extrapolation of this kind can hardly ever drive it to zero, so warmth's large $\delta$ of \OsterDeltaW{} is not in itself evidence of robustness. The informative number is a different one: assume unobservables matter exactly as much as the controls, and the warmth coefficient that survives is \OsterBetaEqualW{}. What no measure of this kind can rule out is a confounder unlike the observed controls, working in the opposite direction to the EAL share. Strictness is the more exposed. Adding the controls lowered its coefficient, the extrapolation continues that movement, and under the equal-importance assumption little of the coefficient survives; its $\delta$ of \OsterDeltaS{} sits just above the conventional benchmark of one.

The second measure, the E-value, asks the question without reference to the controls: how strongly a single unmeasured confounder would have to be associated with both the culture score and progress to account for the whole estimate. The answer is an association of at least \EValW{} in risk-ratio terms for warmth and \EValS{} for strictness. Both measures speak only to omitted variables; neither touches reverse causation. Both are also likelier understatements than overstatements, because measurement error in the culture scores biases the estimated coefficients, and the bounds built on them, towards zero.

Schools in the same academy trust share practices, so the standard errors were also computed with clustering on trust membership from the schools register (1,595 clusters; three-fifths of schools in multi-school trusts). Clustering moves the standard errors by at most ten per cent and changes no inference. The visited sample has too few multi-school trusts (25 schools across nine shared trusts) for cluster-robust inference to be informative, so I claim no reassurance from it.

The strictness result does not depend on treating the five bands as equally spaced steps. Comparing each band directly with band 3, progress rises band by band ($-0.27$, $-0.05$, $+0.15$, $+0.43$); \cref{tab:band_dummies} reports the four estimates.

One further doubt concerns the national sample itself. The national regressions run on 3,052 of the 3,332 panel schools, and the footnote with the results gives the arithmetic: 60 schools have no scored inspection report, 80 no published Progress~8, 95 are excluded as late-entry schools, and 45 lack a control variable. The doubt is whether the missing 280 differ from the rest in a way that could bias the estimated associations.

They differ in how well they are doing. The omitted schools are more disadvantaged and weaker than the estimation sample: the uninspected group averages a free-school-meals share of 37 per cent against the sample's 30, and where an outcome is published the omitted groups' mean Progress~8 sits well below zero ($-0.45$ for the uninspected schools, $-0.44$ for the late-entry schools and $-0.27$ for those missing a control, against $+0.01$ in the sample). That is the profile of newly opened schools, schools in transition and university technical colleges, and it says who the estimates describe: established mainstream schools.

Being weaker is not, by itself, a source of bias. The regression estimates how progress moves with strictness among the schools it includes, so leaving out a group of weak schools changes who is described, not the answer for those included. The estimates would only mislead if the omitted schools' own strictness--progress relationship were different, so that putting them back would change it. For one group that can be tested directly. The 45 schools dropped only for a missing control have both a strictness band and an outcome. Among them, a standard deviation of the band score goes with $0.29$ of a grade (standard error $0.07$); in the estimation sample the same figure is $0.23$ ($0.01$). The two are indistinguishable, and the omitted group's is if anything the steeper. For the omissions to inflate the national strictness association, the missing schools would need a weaker relationship than the sample. The one direct test finds the same or a slightly stronger one, so the exclusions read as mildly conservative rather than favourable.

\section{Benchmarks for the National Coefficients}
\label{app:3A:benchmarks}

Each national coefficient has a benchmark. The national score is an imperfect stand-in for the enacted one, so its coefficient should be a scaled-down copy of the visited coefficient, shrunk by the share of the enacted score it captures. For warmth that benchmark is a third of $0.219$, roughly $0.07$, and the national estimate of $\NatW$ sits on it. For teaching it is $0.28 \times 0.206 \approx 0.06$, and the estimate of $\NatT$ sits about a quarter above. For strictness it is $0.30 \times 0.199 \approx 0.06$, and the estimate of $\NatS$ is roughly double. The benchmarks are rough: the shares they rest on come from about a hundred schools of restricted range, so each carries a sampling error of about $0.1$, and the strictness share is lowered by the coarseness of a five-band score, which sets that benchmark too low.

The strictness overshoot has an explanation in how the two readings were built. The warmth and teaching models were trained only on the part of the enacted score that demographics cannot explain, with names and other shortcuts stripped from the text, so their scores carry warmth and teaching and little else. The mark scheme reads the report as it stands. Its bands pick up conduct, and with it some of the general character of an orderly, effective school, and that general character is itself correlated with results. Part of the strictness coefficient is that extra content rather than strictness alone. For the ranking of the two dimensions, the visited sample is the one to trust, because there the dimensions were measured directly, and warmth carries at least as much weight as strictness.

\section{Additional Tables}
\label{app:3A:tables}

\Cref{tab:main_results_s1,tab:main_results_s2,tab:main_results_s3} report the
full five-outcome versions of the three main-body stages, including the EBacc
and Open components. These tables use the primary specification ($n = 99$,
predecessor-filled 2019 grades, late-entry schools excluded), which is
the same one as the main-body tables, so the Overall, English and Maths
columns reproduce the body exactly and the two extra columns can be read on
the same footing.
In the EBacc and Open columns, warmth and strictness are both significant
at the 5 per cent level for EBacc with near-equal magnitudes, and both are
significant for Open as well, again with near-equal magnitudes. The
differential prediction set out in \cref{app:3A:p8mechanics} expected
strictness to show most strongly in the EBacc component and warmth in the
Open component. That prediction is not supported: both dimensions are
associated with both components to a similar degree. As \cref{app:3A:p8mechanics} explains, both components mix progress with
the school's entry decisions, so the entry-rate analysis in
\cref{tab:entry_rates} is the clearer way to answer the pathway question.
The strictness score predicts EBacc, humanities and languages entry nationally, by between 1.4 and 3.3 percentage points per band standard deviation, with or without the grade control. The attenuation figure
quoted in the body of the chapter comes from these regressions: adding the EBacc entry rate to the EBacc specification reduces the strictness coefficient by 10 to 14~per~cent, so entry itself carries little of the association.

\input{tables/tab_main_results_s1}
\input{tables/tab_main_results_s2}
\input{tables/tab_main_results_s3}
\input{tables/tab_entry_rates}

\Cref{tab:outcome_stats_full} reports descriptive statistics for all five
Progress~8 components and the four Attainment~8 buckets, and
\cref{tab:national_strictness_full} the national strictness estimate on
all five outcomes. The coefficients run from $0.11$ (Maths) to $0.15$
(Open), all precise, so no single component drives the overall result.

\input{tables/tab_outcome_stats_full}
\input{tables/tab_national_strictness_full}

\Cref{tab:subscores} enters the five observational blocks of the enacted scores jointly, and \cref{tab:items_fdr} takes the decomposition to the
level of the 33 individual observation items, each entered in its own
small model with a false-discovery correction across the set and each
item's inter-rater reliability shown beside it. Every one of the 33 items has a positive coefficient and 28 survive the false-discovery correction. The sub-score result reported in the body, that only outside-lesson warmth and in-lesson strictness keep their associations when entered together, therefore says which blocks carry information of their own, not which items relate to progress at all: nearly all of them do.

\input{tables/tab_subscores}
\input{tables/tab_items_fdr}
\input{tables/tab_band_dummies}

\Cref{tab:typology} reports the full combination analysis behind the
body's additive result, for both the visited sample and the national
panel.

Entering predicted warmth and the bands together (\cref{tab:typology}) moves warmth from $+0.078$ to $+0.054$ while the bands stay at $+0.117$, consistent with the two readings picking up some of the same features of a report. The univariate estimates remain the primary numbers because, as the body explains, the two readings do not carry the same mix of content.

\Cref{tab:gaps} examines whether disagreement between two measures of the same school predicts progress. It sets espoused
against enacted scores in the visited sample, and document scores against the inspection-report scores nationally. Disagreement predicts nothing. The final columns explain the two gap terms that look significant: they track the square of the enacted score, so what they pick up is schools with extreme scores, not schools whose words and behaviour disagree.

\input{tables/tab_typology}
\input{tables/tab_gaps}

\Cref{tab:llm_p8_matrix} reports, for completeness, the association of every
text-derived score with the five outcomes under the grade-controlled
specification. Only the inspection-report scores have substantial
associations that survive the grade control and the false-discovery
correction. The website binary indicators have small ones, and the behaviour-policy warmth score is negative, surviving the false-discovery correction only for the Open component: schools whose policies sound warmest do slightly worse, consistent with the previous chapter's finding that policy language describes aspiration rather than practice. \Cref{tab:parentview} gives the parent-survey panel.

\input{tables/tab_llm_p8_matrix}
\input{tables/tab_parentview}

The pseudo-progress measure of \cref{tab:p8_proxy} is validated by running
the identical construction one year earlier, where it correlates with the
published Progress~8 at $+0.81$. The measure is an imputed value-added
outcome for the one cohort measured contemporaneously with the fieldwork.
As the body reports, the culture coefficients on the pseudo-outcome sit
within $0.04$ of their overall real-P8 counterparts.

\Cref{tab:stability_p8} supports the outcome-year strategy directly:
within-school Progress~8 movement across years is small. The academisation
event-time columns are included because conversion is the main discrete
event that could break the assumption of persistent culture, and they show
why conversion effects must be read against conversion type. Sponsor-led schools decline before
converting and recover afterwards, which is the pattern one would expect
from regression to the mean, while converters are flat throughout.

\input{tables/tab_p8_proxy}
\input{tables/tab_stability_p8}

\Cref{tab:semh_mechanism} tests the sorting concern directly, regressing the
2023--24 share of pupils with social, emotional and mental-health needs on
strictness, conditional on the school's 2015--16 share and the standard
controls. Nationally, stricter schools have a lower current share conditional
on their baseline ($-0.108$ of a percentage point per standard deviation of
the score, $p = 0.013$, $n = 2{,}594$); the direction is the same in the visited
sample but imprecise ($-0.257$, $p = 0.36$, $n = 94$), and warmth, which is
available only for the visited sample, is unrelated to the change there. The pattern is consistent
with sorting, though it cannot distinguish selective admission from schools
supporting pupils to leave the register. Adding the baseline share to the
primary specification (\cref{tab:robustness_overall}) moves the
strictness coefficient from $0.106$ to $0.090$; re-estimating on the same 94
schools without the control gives $0.097$, so about seven per cent of the
strictness association is attributable to the control itself and the rest of
the movement to the change of sample.

\input{tables/tab_semh_mechanism}

\Cref{tab:score_controls_corr} reports pairwise Pearson correlations between
the three enacted culture scores ($W_{\text{enac}}$,
$S_{\text{enac}}$, $T_{\text{enac}}$) and the two espoused scores
($W_{\text{esp}}$, $S_{\text{esp}}$) against each control variable,
estimated on the \NTierOne{} visited schools. No pairwise
correlation exceeds $|r| = 0.29$. The largest entries in the
table are between espoused strictness and KS2 prior attainment
($r = +0.29$), between $T_{\text{enac}}$ and selective status
($r = +0.27$) and EAL share ($r = -0.26$), and between $W_{\text{enac}}$
and EAL share ($r = -0.25$). This is the detail behind the
body's statement that the culture scores are not close proxies for intake
or school type. Selective
schools score modestly higher on both culture dimensions ($r = +0.20$ for
warmth and $+0.22$ for strictness), which the selective-admissions control
absorbs in every specification.

\input{tables/tab_score_controls_corr}

A final check re-estimates the Stage~1 specification on the subset of
schools where the headteacher named in the most recent inspection report is
confirmed still in post in the July~2026 GIAS snapshot. Continuity could be determined for only 29 of the 99 estimation-sample schools, of which 22 were
unchanged and 7 had changed, so the restriction is driven as much by data
coverage as by turnover. The strictness point
estimates are in the same direction as the full-sample estimates and are
larger in every component. However, neither warmth nor strictness is
significant at the 5 per cent level in any component, and, as the body
reports, the subsample estimates cannot be read directionally. This check
does not provide evidence against the primary results, but the subsample
is too small to detect effects of the size found in the full
specification.

\Cref{tab:sensitivity_bounds} reports the sensitivity measures quoted in
the robustness discussion: Oster's $\delta$ and the equal-selection bound
for each culture score, computed with \texttt{psacalc} under
$R_{\max} = \SensRmax$, and the E-values for the point estimates and for
the confidence-interval boundaries.

\input{tables/tab_sensitivity_bounds}
