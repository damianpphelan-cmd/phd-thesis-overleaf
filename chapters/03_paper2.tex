\chapter{School Culture and Pupil Progress: Evidence from English Secondary Schools}
\label{ch:paper2}
\resetcounterformats

\begin{center}
  \textit{Damian Phelan%
    \thanks{Department of Economics, University College London.
      [\textit{INSERT: acknowledgements.}]}}
\end{center}

\begin{chapterabstract}
Does the culture of a school affect how much its pupils learn? This paper
estimates the association between the warmth and strictness of English
secondary schools, measured by direct observation at \NTierOne{} visited
schools, and Progress~8, the Department for Education's value-added measure,
conditioning on prior attainment, intake composition, school type, size,
location and the school's pre-pandemic inspection grade. Three results follow.
Both dimensions predict progress with about equal weight: a standard deviation
of observed warmth is associated with $\hat{\beta}_W = \PrimarySpecW$ of a grade
higher Progress~8 and a standard deviation of observed strictness with
$\hat{\beta}_S = \PrimarySpecS$ ($n = \PrimarySpecN$), both significant at the
one per cent level and stable across specifications. The association does not
run through observed teaching quality, which enters at $0.024$ and leaves both
culture coefficients unchanged. And the dimensions add rather than amplify one
another: the warm-and-strict schools sit $0.36$ of a grade above the rest and
the interaction is null. Substituting the headteacher's own account for the
observed scores removes the warmth association, and the strictness score read
from inspection reports reproduces the strictness result across
\NNationalStrictness{} schools with the inspection grade held constant
($\BetaNationalStrictness$).
\end{chapterabstract}

% ---------------------------------------------------------------
\section{Introduction}
\label{sec:p2_intro}

Does the culture of a school affect how much its pupils learn? This paper
estimates the association between the warmth and strictness of English
secondary schools, measured by direct observation, and the progress their
pupils make between the end of primary school and GCSE. It uses the enacted
culture scores constructed in the previous chapter for \NTierOne{} visited
schools, and the Progress~8 measure published by the Department for
Education, which records each school's average pupil progress relative to
pupils nationally with the same prior attainment.

The question sits in the school-effectiveness literature, which since
\citet{coleman1966} and \citet{rutter1979} has asked how much of the variation
in pupil outcomes is attributable to schools rather than to intake, and which
features of a school carry it. Two strands are closest to this paper. The
authoritative-school literature \citep{pellerin2005,gregory2010} finds that
schools combining support with structure produce better behavioural and
engagement outcomes, but has rarely had an academic-progress outcome or a
direct measure of both dimensions. And the literature on high-expectation,
high-structure charter and academy models
\citep{angrist2010,dobbie2013,fryer2014,eyles2019} finds large effects of a
bundle of practices in which strict behaviour norms and warm, individual
relationships travel together, without separating them. This paper measures
the two dimensions separately, at ordinary schools rather than a selected
model, and asks what each contributes.

The design is an association with several features that strengthen it short
of identification. The outcome is value-added, so it already conditions on
where pupils started; the culture measures come from documents and visits that
mostly predate or coincide with the outcome cohort; the regressions condition
on intake composition, prior attainment, school type, size, location and the
school's pre-pandemic inspection grade; and the sample excludes schools that
admit pupils only at 13 or 14, whose Progress~8 attributes to them progress
they cannot have caused. What the design cannot do is rule out that some
persistent characteristic of a school produces both its culture and its
results: a headteacher who is effective in ways the visit does not measure, or
a stable school reputation that draws both committed staff and motivated
families. Two features of the data bear on this without resolving it. Progress
is stable within schools across years, so the culture measured at the visit is
a fair proxy for the culture of the outcome cohort; and the strictness result
reproduces across the national population using a text-based measure with the
inspection grade held constant, so it is not an artefact of which schools
agreed to be visited. The estimates are nonetheless presented as associations
throughout, and no coefficient is described as an effect.

Three results follow. First, both dimensions predict progress, with about
equal weight: a one standard deviation increase in observed warmth is
associated with $0.14$ of a grade higher Progress~8, and in observed
strictness with $0.13$, both significant at the one per cent level, and both
stable across every treatment of the inspection-grade control. Entered alone
each dimension carries a coefficient of about $0.22$, so the two share some
variance but each carries independent signal. Second, the association does not
run through observed teaching quality: adding the visit's teaching score
leaves both culture coefficients unchanged and the teaching coefficient
itself is close to zero. Third, the schools that combine high warmth with high
strictness outperform the rest by a third of a grade, while the interaction
between the two dimensions is null; the dimensions add rather than amplify
one another. Substituting the headteacher's own account of the school for the
observed scores removes the warmth association entirely, and the strictness
score read from inspection reports reproduces the strictness result across
the national population with the inspection grade held constant.

The paper proceeds as follows. Section~\ref{sec:p2_data} describes the
outcome, the culture measures, the controls and the sample.
Section~\ref{sec:p2_spec} sets out the specification.
Section~\ref{sec:p2_results} reports the results and their robustness.
Section~\ref{sec:p2_conclusion} concludes.

% ============================================================
\section{Data}
\label{sec:p2_data}

\textbf{Outcomes.} The outcome is Progress~8, the Department for Education's
school-level value-added measure: each pupil's attainment across eight GCSE
slots is compared with the national average for pupils with the same Key
Stage~2 starting point, and the differences are averaged across the school. A
school at zero is exactly average; $+0.5$ means half a grade more per subject
than its intake would predict. The paper reports overall Progress~8 and its
English and Maths components, which are the cleanest measures the tables offer
because every pupil sits both examinations. The remaining components, EBaC and
Open, are partly determined by which subjects a school enters pupils for, and
are reported in \cref{app:3A:tables} together with a direct analysis of subject
entry rates, and \cref{app:3A:p8mechanics} sets out how the components are
constructed. The primary outcome is
the two-year average of the 2022/23 and 2023/24 cohorts, the two post-pandemic
years for which a Key Stage~2 baseline exists; the 2024/25 cohort has no
baseline, and \cref{app:3A:tables} reports a validated pseudo-progress measure
for it.

\textbf{Culture measures.} The explanatory variables are the enacted warmth
and strictness scores of the previous chapter: the visit-based measures, on a
0--10 scale, for the \NTierOne{} visited schools, entered per standard
deviation. The visit's teaching-practice score enters as a benchmark. The
espoused scores enter one robustness specification and the inspection-report
strictness score the national extension.

\textbf{Controls.} All specifications condition on prior attainment (mean Key
Stage~2 score of the cohort), the shares of pupils eligible for free school
meals, with English as an additional language and with special educational
needs, log school size, academy status, urban location, selective admissions,
years since the last inspection, and the school's overall inspection grade as
at August 2019, entered as dummies. The 2019 grade is used rather than the
current one because a contemporaneous grade is partly an outcome of the
culture being measured. Six visited schools converted to academies after 2019
and carry no grade of their own; for five, the predecessor school's 2019 grade
is used, and the sixth is dropped where the control is required.

\textbf{Sample.} Two exclusions apply. Schools whose statutory entry age is 13
or above (university technical colleges, studio schools and similar; 98
schools nationally, two among those visited) are excluded from every outcome
regression: Progress~8 measures progress from age 11, and these schools enrol
their pupils at 13 or 14. One visited school lacks the years-since-inspection
control. The primary estimation sample is therefore $n = \PrimarySpecN$.
\Cref{tab:outcome_stats} and \cref{tab:control_stats} report descriptive
statistics for outcomes and controls; the correlations between the culture
scores and the controls are all below $|0.3|$ (\cref{tab:score_controls_corr}).

\input{tables/tab_outcome_stats}

\input{tables/tab_control_stats}

The visited schools lean toward stronger schools than the national
population, as the previous chapter records, so the estimates apply most
directly to a population somewhat above the national average, and the
restricted range of both culture and outcomes works against finding
associations.

% ============================================================
\section{Empirical Specification}
\label{sec:p2_spec}

The primary specification is
%
\begin{equation}
  \label{eq:main}
  Y_s = \alpha + \beta_W W_s + \beta_S S_s + \mathbf{X}_s'\gamma
        + \sum_g \delta_g \,\mathbb{1}[G_s = g] + \varepsilon_s ,
\end{equation}
%
where $Y_s$ is the two-year average Progress~8 (overall, English or Maths) of
school $s$, $W_s$ and $S_s$ are its enacted warmth and strictness scores in
standard-deviation units, $\mathbf{X}_s$ the controls listed above and $G_s$
its 2019 inspection grade. Standard errors are heteroskedasticity-consistent
(HC3). Three stages are estimated: equation~(\ref{eq:main}); the same
equation with the teaching-practice score $T_s$ in place of $W_s$ and $S_s$;
and the equation with all three. Each dimension is also entered alone. The
national extension replaces $W_s$ and $S_s$ with the inspection-report
strictness score, standardised, on all schools with an outcome, and is
estimated with and without the grade dummies.

% ============================================================
\section{Results}
\label{sec:p2_results}

\textbf{Culture predicts progress.} \Cref{tab:spec_ladder} reports the
primary estimates. In the primary specification observed warmth carries a
coefficient of $\hat{\beta}_W = \PrimarySpecW$ ($p = 0.002$) and observed
strictness $\hat{\beta}_S = \PrimarySpecS$ ($p = 0.007$) on overall
Progress~8, with $R^2 = 0.62$. The table shows the
same equation under four treatments of the grade control, from requiring an
original 2019 grade ($n = 95$) to dropping the control ($n = 100$); the
coefficients move by less than $0.02$ across the four. The two dimensions
contribute with near-equal weight, as the authoritative-school framework
predicts.

\input{tables/tab_spec_ladder}

Entered alone, each dimension carries a coefficient of about $0.22$
(\cref{tab:univariate_ws}): $0.218$ for warmth and $0.228$ for strictness.
Jointly each retains roughly 60 per cent of its solo coefficient. The two
dimensions therefore share variance, as their correlation of
$\CorrWarmthStrictnessEnacted$ implies,
but each carries substantial signal the other does not, which is the
empirical basis for treating them as two constructs.

\input{tables/tab_univariate_ws}

At the component level English is warmth-led (warmth $0.167$, $p < 0.001$;
strictness $0.087$, $p = 0.083$) and Maths balanced (warmth $0.112$,
$p = 0.041$; strictness $0.110$, $p = 0.036$). The five-outcome tables in
\cref{app:3A:tables} show both dimensions significant for the EBaC and Open components; the appendix also shows that the strictness
score predicts entry to the EBacc pathway nationally with the grade held
constant, but that controlling for entry attenuates the strictness--EBaC
association by only 10 to 14 per cent. Most of the association is progress
within subjects rather than curriculum steering.

\textbf{Teaching quality does not account for it.} \Cref{tab:stages23_trio}
reports the second and third stages. The visit's teaching-practice score alone
predicts overall Progress~8 at $0.245$ ($p < 0.001$) but with a lower $R^2$
($0.55$) than the two culture scores. Added to the primary specification it
enters at $0.024$ ($p = 0.76$), and the culture coefficients are unchanged
($0.131$ and $0.126$). Warmth for Maths slips past the five per cent threshold
once teaching enters ($p = 0.115$), which is unsurprising given how much
variance the three visit scores share. Culture and observed instruction are
largely distinct predictors of progress; conditioning on teaching quality does
not account for the culture association, though with cross-sectional data the
alternative reading, that teaching is the noisiest of the three visit scores
and its signal is absorbed by the other two, cannot be excluded.

\input{tables/tab_stages23_trio}

\textbf{The dimensions add.} \Cref{fig:quadrant_ws} plots the visited schools
by their two culture scores, shaded by progress. Schools in the warm and
strict quadrant are predominantly above-average performers. Formally, the
warm-and-strict quadrant sits $0.36$ of a grade above the rest of the sample
after controls ($p < 0.001$), while the interaction between the two
standardised scores is null ($0.045$, $p = 0.25$;
\cref{tab:robustness_overall}). Warmth and strictness add
rather than amplify one another; the best schools have both because both help,
not because of a combination effect. Decomposing the visit scores into their
five observational blocks (\cref{tab:subscores}), the association runs
through warmth outside lessons and orderly conduct within them: out-of-lesson
warmth ($0.107$, $p = 0.017$) and in-lesson strictness ($0.118$, $p = 0.006$)
survive joint entry, and their counterparts do not.\footnote{The quadrant
contrast and the sub-score decomposition are estimated on the primary control
set without the years-since-inspection term ($n = 100$); the interaction is
from the primary specification. None of the three conclusions depends on the
difference.}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.85\textwidth]{fig_quadrant_ws.pdf}
  \caption{Enacted warmth and strictness for the visited schools, shaded by
  Progress~8 (two-year average). Dashed lines mark sample medians. The
  adjusted quadrant comparison is in \cref{tab:typology}.}
  \label{fig:quadrant_ws}
\end{figure}

\textbf{Espoused culture does not predict progress.} Substituting the
headteacher's espoused scores for the observed ones on the same schools
reduces the warmth coefficient to $0.036$ ($p = 0.47$); espoused strictness
retains a smaller association ($0.113$, $p = 0.019$). The measurement
asymmetry of the previous chapter reappears as a predictive one: what a
headteacher says about warmth carries no information about progress, and what
they say about behaviour systems carries some. Two related results are in
\cref{app:3A:tables}. The gap between a school's espoused and enacted scores predicts
nothing beyond the enacted level; and the association is not confined to the
visited schools' own report on themselves, since parents' survey responses on
their children's happiness and safety are positively associated with progress
($0.14$ per standard deviation) with the caveat that satisfaction and results
are jointly determined.

\textbf{National extension.} The inspection-report strictness score, which
the previous chapter showed to track observed strictness at
$r = \CorrOfstedStrictness$, is
available for \NNationalStrictness{} schools with an outcome. On the full
control set without
the grade it carries $\BetaNationalStrictness$ ($p < 0.001$, $R^2 = 0.52$;
\cref{tab:national_strictness}); with the 2019 grade added ($n = 2{,}823$) it
carries $0.167$, and no component estimate moves by more than $0.005$. A score
read from an inspection report might be thought to proxy for the inspectorate's
overall approval; that the coefficient is untouched by conditioning on the
grade itself is difficult to reconcile with that reading. Two further
text-derived results are consistent with the previous chapter's measurement
findings and are reported in \cref{tab:llm_p8_matrix}: the behaviour-policy
warmth score is significantly \emph{negatively} associated with progress, and
the website warmth and strictness scores are not associated with it at all.

\input{tables/tab_national_strictness}

\textbf{Robustness.} \Cref{tab:robustness_overall} reports the primary
estimate under alternative specifications, all on the primary sample. Dropping
the grade control ($0.140$, $0.127$; $n = 100$), using the single 2023/24
outcome year ($0.158$, $0.126$), and adding the 2015/16 baseline share of
pupils with social, emotional and mental-health needs ($0.156$, $0.105$;
$n = 94$) all leave both coefficients significant at the five per cent level
or better. On the 2024/25 cohort's Attainment~8, which does not condition on
intake, both dimensions remain significant, on the total of the four buckets
($1.17$, $1.73$ points) and on the English bucket alone ($0.31$, $0.22$). Restricting to the 22 schools
whose headteacher is confirmed unchanged since the last inspection leaves the
strictness point estimates at or above the full-sample values and the warmth
estimates imprecise; the subsample is too small to be informative
(\cref{tab:continuity_robustness}). The
sorting concern, that strict schools have fewer pupils with social, emotional
and mental-health needs, accounts for under a tenth of the strictness
association (\cref{tab:semh_mechanism}). Within-school Progress~8 is stable
across years
(mean within-school standard deviation $0.20$), which supports using culture
measured after the outcome window as a proxy for culture during it
(\cref{tab:stability_p8}); the 2024/25 pseudo-progress measure, the one cohort
measured contemporaneously with the fieldwork, gives coefficients within
$0.04$ of their real-Progress~8 counterparts (\cref{tab:p8_proxy}).

\input{tables/tab_robustness_overall}

\textbf{Implications.} Two implications bear on current policy debate and
follow from the pattern of results rather than any single coefficient. The
behaviour debate treats discipline as the active ingredient of school
improvement and relational culture as secondary; these estimates give the two
equal weight, show no trade-off between them, and place the warm-and-strict
schools a third of a grade ahead. And the association runs through relational
quality outside lessons and orderly conduct within them, which locates the
relational work that matters outside the classroom, where observation and
training effort are least concentrated. Both implications are conditional on
an association being at least partly causal, which the design supports but
does not establish.

% ============================================================
\section{Conclusion}
\label{sec:p2_conclusion}

Observed school culture predicts pupil progress. Across \NTierOne{} visited
English secondary schools, a standard deviation of observed warmth is
associated with $0.14$ of a grade higher Progress~8 and a standard deviation
of observed strictness with $0.13$, both significant at the one per cent
level and both stable across every reasonable specification. The two
dimensions contribute equally and additively; the schools that combine them
outperform the rest by a third of a grade. Observed teaching quality does not
account for the association. The headteacher's own account of the school
predicts nothing on warmth, and the strictness score read from inspection
reports reproduces the strictness result nationally with the inspection grade
held constant.

The estimates are associations, strengthened by a value-added outcome,
temporal ordering, a rich control set and stability across specifications, but
not identified: a persistent characteristic that produces both a school's
culture and its results cannot be excluded with cross-sectional data. The
visited sample is small and leans toward stronger schools; culture was
measured after the outcome window for most schools; and one visit is a noisy
reading of a school. Within those limits, the results locate the relational
and disciplinary features of a school that travel with its pupils' progress,
and they show that one of the two, strictness, is legible at national scale
in the public record.

% ===============================================================
%  APPENDIX 3A
% ===============================================================
\clearpage
\startchapterappendix{3}
\begin{center}
  {\LARGE\bfseries Appendix to Chapter 3}\\[6pt]
  \rule{0.5\linewidth}{0.4pt}
\end{center}
\medskip
\noindent This appendix collects the supporting material for the analyses
reported in the chapter: the structural-stability evidence that underpins the
outcome-year strategy, the full five-outcome versions of the main results
tables (the chapter body reports overall, English and Maths Progress~8; the
EBaC and Open components appear here), the secondary and exploratory
analyses, and a short section explaining how the Progress~8 measures and the
subject entry rates are calculated.
\medskip

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\section{How the Progress 8 Measures Are Calculated}
\label{app:3A:p8mechanics}

Progress~8 measures the progress a pupil makes between the end of primary
school and GCSE, relative to pupils nationally who started from the same
point. Each pupil's GCSE results are counted across eight slots, their
attainment is compared with the national average attainment of pupils with
the same Key Stage~2 starting point, and the difference is averaged across
the school. A school at zero is exactly average; a school at $+0.5$ achieves
half a grade more per subject per pupil than would be expected from its
intake.

The eight slots are structured, and the structure is why this thesis treats
the components differently. English and Maths each fill two slots and are
double-weighted; every pupil sits both, so these components measure progress
and nothing else. The EBaC component covers three slots that must be filled
from a restricted academic list: sciences, computer science, history,
geography, and modern foreign languages. The Open component covers the final
three slots, which may be filled by any approved qualification, including
arts and vocational subjects. For these last six slots the school makes
entry decisions, and an unfilled slot contributes zero progress to the
school's average. Consequently, a school's EBaC and Open components mix two
different things: how much progress pupils make in the subjects they enter,
and which subjects the school enters them for. A school that enters few
pupils for languages will carry empty EBaC slots and a depressed EBaC
component even if the pupils it does enter progress well.

This structure generates a theoretical prediction about warmth and
strictness. Schools higher on strictness, characterised by structured
behaviour management, high academic expectations, and intensive pupil
steering, are more likely to direct pupils into the EBaC pathway, and could
therefore raise their EBaC component through the entry-rate channel as well
as through progress itself. Schools higher on warmth might generate stronger
progress in the more elective Open component. The five-outcome versions of
the main results tables in this appendix speak to that prediction, and the
entry-rate analysis below addresses the pathway channel directly.

The entry rates themselves are calculated from the pupil count columns of
the 2023--24 performance tables, because the Department for Education
stopped publishing percentage-form entry columns in recent releases: the
EBaC entry rate is the number of pupils entered for the full EBaC suite
divided by the number of pupils at the end of Key Stage~4, and the
humanities and languages rates are constructed in the same way from their
respective counts. Entry rates are available only at the subject-group
level; individual subjects within a group cannot be distinguished in the
data collected for this project.

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\section{Structural Stability of School Characteristics}
\label{app:3A:stability}

The intra-class correlation (ICC) decomposes total variance in a panel variable
into a between-school component and a within-school over-time component. An ICC
close to one indicates that schools are much more different from each other than
they are from their own past selves: year-on-year within-school fluctuation is
small relative to persistent inter-school heterogeneity. ICC is estimated by
one-way random-effects ANOVA on the school-year panel, retaining only schools
with at least three years of non-missing data for the variable in question.

\Cref{tab:structural_stability} reports ICC estimates and mean absolute
year-on-year changes for nine panel variables covering pupil intake composition,
special educational needs prevalence, prior attainment, pupil progress, and
per-pupil expenditure. Six of nine variables exceed ICC~$= 0.69$; all
exceed~$0.49$. Intake characteristics---the share of pupils with English as an
Additional Language, school roll, and mean KS2 prior attainment of the entering
cohort---are especially stable, with ICC values between 0.81 and 0.92 and mean
small mean absolute year-on-year changes relative to their between-school spread.
Special educational needs prevalence (EHC plan share, ICC~$= 0.67$; SEN support
share, ICC~$= 0.65$) and school-level Progress~8 (ICC~$= 0.73$) show moderate to
high stability. Per-pupil expenditure, at ICC~$= 0.52$, is the most volatile,
reflecting sensitivity to national funding policy changes; even so, the majority
of variation in expenditure is between schools rather than within schools over
time.

These patterns are consistent with the view that schools occupy relatively
persistent positions in the distribution of structural characteristics, and
support the use of culture scores measured in 2024--25 as proxies for the
organisational conditions present during the 2023--24 outcome period.

\Cref{tab:sen_category_stability} extends the analysis to the thirteen
primary-need categories within the SEN population. ICC values range from
0.320 (Severe Learning Difficulty) to 0.643 (Moderate Learning Difficulty);
the educationally important Social, Emotional and Mental Health (SEMH)
category has ICC~$= 0.545$. The lower stability of SEMH relative to the
aggregate SEN support share (ICC~$= 0.65$) is consistent with SEMH composition
being partly responsive to school behaviour and exclusion practices, rather than
being determined solely by fixed intake characteristics. Low ICCs for rare
categories (Severe Learning Difficulty, Multi-Sensory Impairment, Visual
Impairment) partly reflect small-count measurement noise rather than genuine
volatility. For the main analysis, aggregate SEN controls are preferred to
category-level decompositions; the moderate stability of baseline SEMH share
supports its inclusion as an additional control in the robustness specification
of \cref{tab:robustness_overall}.

\input{tables/tab_structural_stability}

\input{tables/tab_sen_category_stability}

\Cref{tab:p8_component_stability} reports ICC estimates separately for the four
Progress~8 components that serve as the primary outcomes of this chapter,
together with the corresponding Attainment~8 components used in the
contemporaneous robustness specification. Among the Progress~8 components,
stability varies from ICC~$= 0.587$ for the Open component to ICC~$= 0.748$
for EBaC, with English and Maths sitting between these at ICC~$\approx 0.67$.
The higher stability of EBaC relative to Open reflects the fact that
curriculum-pathway decisions---which subjects pupils are entered for---are
governed by stable school policy and change little from year to year, while
the more eclectic Open bucket (arts, vocational, and additional GCSEs) is
subject to greater annual variation in subject mix. All four P8 components
exceed the per-pupil expenditure ICC of~$0.52$, which is the weakest result
in the main stability table; the cross-sectional design is therefore at least
as defensible for each component individually as for the structural
characteristics already discussed.

The theoretical prediction of this chapter is that warmth is most strongly
associated with the Open component---the dimension most sensitive to breadth
of provision and pupil engagement---and that strictness is most strongly
associated with the EBaC component---the dimension most sensitive to academic
pathway steering. The lower stability of Open P8 relative to EBaC P8
(ICC~$= 0.587$ versus $0.748$) implies that the warmth coefficient will be
estimated against a noisier outcome, which may attenuate the estimated effect.
This is a limitation to acknowledge in interpreting any differential finding
between components.

Attainment~8 component ICCs substantially exceed their Progress~8 counterparts
across all four components, ranging from ICC~$= 0.750$ (Open) to
ICC~$= 0.887$ (EBaC). This pattern is expected: Attainment~8 measures absolute
performance without subtracting KS2 prior attainment, so the stable intake
signal---itself at ICC~$= 0.91$ for the KS2 scaled score---is retained in the
dependent variable and inflates between-school variance relative to
within-school variance. The higher Attainment~8 ICCs therefore do not indicate
that value-added is more stable than raw attainment; they reflect the
composition of the variance rather than the reliability of the school-level
signal.

\input{tables/tab_p8_component_stability}

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\section{Additional Tables}
\label{app:3A:tables}

\Cref{tab:main_results_s1,tab:main_results_s2,tab:main_results_s3} report the
full five-outcome versions of the three main-body stages, including the EBaC
and Open components. These tables carry the primary specification ($n = 99$,
predecessor-filled pre-COVID grades, late-entry schools excluded), the same one
as the main-body tables, so the Overall, English and Maths columns reproduce
the body exactly and the two extra columns are read on the same footing.
Reading the EBaC and Open columns, warmth and strictness are both
significant for EBaC with near-equal magnitudes, and both are significant for
Open as well, again with near-equal magnitudes. As \cref{app:3A:p8mechanics} explains, both components mix progress
with the school's entry decisions, so the entry-rate analysis in
\cref{tab:entry_rates} is the cleaner way to read the pathway question: the
strictness score predicts EBacc, humanities and languages entry nationally
with the inspection grade held constant, and controlling for EBacc entry
attenuates the strictness--EBaC association by only 10 to 14~per~cent. Most
of that association is progress within subjects, not curriculum steering.

\input{tables/tab_main_results_s1}
\input{tables/tab_main_results_s2}
\input{tables/tab_main_results_s3}
\input{tables/tab_entry_rates}

\Cref{tab:subscores} decomposes the visit-based culture scores into their
five observational blocks and enters them jointly. Only two survive:
outside-lesson warmth and in-lesson strictness. The progress association
runs through relational quality in corridors and transitions, and orderly
conduct in classrooms, rather than through their counterparts.
\Cref{tab:items_fdr} takes the decomposition to the level of individual
observation items, each entered in its own small model with a
false-discovery correction across the set and each item's inter-rater
reliability shown beside it; 28 of the 33 items carry a positive association
individually, so the sub-score result reflects which facets carry
\emph{independent} signal, not which items relate to progress at all.

\input{tables/tab_subscores}
\input{tables/tab_items_fdr}

\Cref{tab:typology} reports the combination analysis. In the visited sample
the warmth-by-strictness interaction is null while the warm-and-strict
quadrant tops the adjusted means, so the two dimensions add rather than
amplify; the national panel using the inspection-report scores shows the
same additive pattern once the grade is controlled. \Cref{tab:gaps} examines
whether the gap between a school's espoused and enacted culture predicts
progress; it does not, and the one apparently significant gap term is shown
in the final columns to be an artefact of document-score extremity rather
than of mismatch itself.

\input{tables/tab_typology}
\input{tables/tab_gaps}

\Cref{tab:llm_p8_matrix} reports, for completeness, the association of every
text-derived instrument with the five outcomes under the grade-controlled
specification. Only the inspection-report scores carry substantial robust
associations; the website binary indicators carry small ones; and the
behaviour policy warmth score is significantly negative, the
policy-as-aspiration finding of \cref{ch:paper1} reappearing as a
predictive result. \Cref{tab:parentview} gives the parent-survey panel.

\input{tables/tab_llm_p8_matrix}
\input{tables/tab_parentview}

\Cref{tab:p8_proxy} reports the 2024--25 pseudo-progress robustness check:
an imputed value-added outcome for the one cohort measured contemporaneously
with the fieldwork, validated by running the identical construction one year
earlier, where it correlates with the published Progress~8 at $+0.81$. The
culture coefficients on the pseudo-outcome sit within $0.04$ of their
real-P8 counterparts. \Cref{tab:stability_p8} supports the outcome-year
strategy directly: within-school Progress~8 movement across years is small,
and the academisation event-time columns show why conversion effects must
be read against conversion type --- sponsor-led schools decline before
converting and recover afterwards, the signature of regression to the mean,
while converters are flat throughout.

\input{tables/tab_p8_proxy}
\input{tables/tab_stability_p8}

\Cref{tab:semh_mechanism} tests the sorting concern directly, regressing the
2023--24 share of pupils with social, emotional and mental-health needs on
strictness, conditional on the school's 2015--16 share and the standard
controls. Nationally, stricter schools have a lower current share conditional
on their baseline ($-0.166$, $p < 0.001$, $n = 2{,}909$); the direction is the
same in the visited sample but imprecise ($-0.291$, $p = 0.37$, $n = 95$), and
warmth is unrelated to the change in either sample. The pattern is consistent
with sorting, though it cannot distinguish selective admission from schools
supporting pupils to leave the register. Adding the baseline share to the
primary specification (\cref{tab:robustness_overall}) moves the
strictness coefficient from $0.131$ to $0.105$; re-estimating on the same 94
schools without the control gives $0.114$, so about eight per cent of the
strictness association is attributable to the control itself and the rest of
the movement to the change of sample.

\input{tables/tab_semh_mechanism}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.85\textwidth]{fig_academisation_event.pdf}
  \caption{Mean Progress~8 in event time around academy conversion, shown
  separately for converter and sponsor-led academies, with predecessor-school
  years linked through the register of predecessor and successor URNs.
  Sponsor-led schools decline before converting and recover afterwards, which
  is the signature of regression to the mean following intervention in
  struggling schools; converters are flat throughout. Any analysis of
  conversion effects that pools the two types would mistake this selection
  pattern for an effect of conversion itself.}
  \label{fig:academisation_event}
\end{figure}

\Cref{tab:score_controls_corr} reports pairwise Pearson correlations between
the three enacted culture scores ($W_{\text{enac}}$,
$S_{\text{enac}}$, $T_{\text{enac}}$) and the two espoused scores
($W_{\text{esp}}$, $S_{\text{esp}}$) against each control variable,
estimated on the Tier~1 sample of \NTierOne{} visited schools. No pairwise
correlation exceeds $|r| = 0.29$. The largest entries are between
$W_{\text{enac}}$ and EAL share ($r = -0.29$), between $T_{\text{enac}}$ and
EAL share ($r = -0.26$) and KS2 prior attainment ($r = +0.24$), and between
$W_{\text{enac}}$ and selective school status ($r = +0.20$). The absence of
high correlations indicates that the culture
scores are not collinear with the observable school characteristics included
in the regression; selective schools score modestly higher on both culture
dimensions ($r = +0.20$ warmth, $+0.22$ strictness), which the selective
control absorbs in every specification.

\input{tables/tab_score_controls_corr}

\Cref{tab:continuity_robustness} reports the Stage~1 specification
side-by-side for the full Tier~1 sample ($n = 96$) and for the subset of
schools where the headteacher named in the most recent Ofsted inspection report
is confirmed still in post in the July~2026 GIAS snapshot ($n = 22$). The
restriction is driven as much by data coverage as by turnover: of the 103
full-data schools, continuity could be determined for only 29, of which 22 were
unchanged and 7 had changed. Given the
small $n$, coefficients in the continuity subsample are imprecisely estimated
(SEs approximately four times larger than in the full sample), and neither
warmth nor strictness achieves statistical significance in any component.
Strictness point estimates are directionally consistent with---and in every
component larger than---the full-sample estimates ($\hat{\beta}_S$ ranging from
0.140 to 0.289), while warmth estimates are noisy and should not be
interpreted directionally. This check does not provide evidence against the
primary results; the subsample is insufficiently powered to detect effects of
the magnitude identified in the full specification.

\input{tables/tab_continuity_robustness}
