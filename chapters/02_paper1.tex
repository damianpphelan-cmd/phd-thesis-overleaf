\chapter{Measuring School Culture: Warmth and Strictness in English Secondary Schools}
\label{ch:paper1}
\resetcounterformats

\begin{center}
  \textit{Damian Phelan%
    \thanks{Department of Economics, University College London.
      Primary supervisor: Michela Tincani. Secondary supervisor: Lars Nesheim.
      [\textit{INSERT: acknowledgements, funding statement.}]}}
\end{center}

\begin{chapterabstract}
How warm a school is towards its pupils, and how strictly it runs, are
widely thought to affect how much progress pupils make. This paper measures both, and asks
how much of each can be extracted from the documents that every school
produces. In \NTierOne{} schools I collected a gold-standard measure: a day
of structured observation recorded what each school does, and an interview
with its headteacher recorded what the head says it does. I then carried out textual analysis of 
every school's Ofsted inspection report, behaviour policy and website using two different approaches. 
Firstly, a prediction model learns, from the visited schools, which words and phrases
are linked with warmer or stricter schools.  Secondly, a large language model (LLM) is asked to 
use a one-page rubric to evaluate the document on each dimension on a 1 to 5 scale. 
There are three findings. First, inspection reports carry a modest signal of how warm a
school actually is: the prediction model gives a correlation of $\PredWOfsted$, even after 
removing what a school's intake statistics explain. Second, we see a similar modest correlation of
of $\RubricSOfstedDec$ after adjusting for pupil intake for strictness measured by the LLM using 
the rubric. Different approaches are required because the two dimensions are written down 
differently: warmth shows in a report's everyday wording, strictness in specific claims that 
have to be asked for.
Third, what schools write about themselves reflects, at most, what their headteachers say: their 
strictness and staff-morale language tracks what the headteacher says when interviewed, 
their warmth language tracks nothing, and neither tracks what observers see. Of 
the \TopQN{} schools whose heads placed them in the top quarter for warmth, 
only \TopQWMatch{} were there when observed. Chance alone would put six or seven there.
\end{chapterabstract}

% ---------------------------------------------------------------
\section{Introduction}
\label{sec:p1_intro}

How warm a school is towards its pupils, and how strictly it runs, are two of
the things most often said to affect how much progress its pupils make. In this paper I construct
measures of these two dimensions of school culture based on a structured day of observations at a school. I refer to these 
two dimensions as warmth and strictness. I then ask how well each dimension can be recovered from publicly available texts that exist for all 3,332 schools in the country.

The two dimensions have precise meanings here. Warmth is
the relational quality of a school: whether pupils are known, cared for and
supported by the adults around them. In a lesson it shows in whether the
teacher uses pupils' names, how praise is given and how the teacher and the
class speak to one another. Between lessons it shows in how staff and pupils
deal with each other in corridors and at break. It also shows in whether rewards are
used consistently across the lessons of the day. Strictness is the school's
behavioural structure: whether
expectations of conduct are clear, enforced and met. In a lesson it shows in
how much misbehaviour is tolerated before a sanction, how much disruption
occurs and how pupils respond when they are corrected. Outside lessons it
shows in how pupils arrive, how they move between lessons and how the dining
hall runs. Strictness in this sense means clear rules applied consistently rather than harsh punishment. A school whose
sanctions are applied by every member of staff in the same way is strict in
this sense.

The two dimensions come from the parenting typology of \citet{baumrind1971}
and \citet{maccoby1983}, which classifies parenting by demandingness and
responsiveness and finds that the combination of both, the authoritative
style, produces the best child outcomes across cognitive and non-cognitive
domains. \citet{majumder2015} and \citet{zhang2020} find the same pattern in
educational attainment, with each dimension carrying an independent effect.
 \citet{pellerin2005} applies the typology to schools, classifying each school as authoritative, 
authoritarian, permissive or indifferent based on its levels of demandingness 
(academic and behavioural expectations) and responsiveness (support for pupils). She finds that 
pupils in authoritative schools have lower dropout and truancy rates.
\citet{gregory2010,gregory2011} use a similar typology and find that more authoritative schools 
haveless bullying, less victimisation and lower suspension rates. \citet{gottfredson2005} finds 
that a school's climate, the clarity and fairness of its rules and how consistently they are 
enforced (demandingness), predicts the level of disorder in the school more strongly than the 
demographics of its pupils. All of these studies rely on surveys of pupils, parents or staff and 
use slight variations on the demandingness and responsiveness language. 
This paper refers to the dimensions as warmth and strictness and measures them through direct observation. 

What makes the two dimensions measurable in a school,
as opposed to in a family, is that both are enacted in public. A researcher
sitting in a lesson can see whether names are used and how disruption is
dealt with, and a researcher standing in a corridor can see whether pupils
move calmly and whether staff speak to them. Neither requires access to
anything private. I treat warmth and strictness as two separate dimensions
rather than as the two ends of one scale, because a school can be warm
without being orderly and orderly without being warm. Whether the two go
together in practice is a question the data answer rather than something I assume.

I also observed a third dimension, the quality of teaching practice.
This is not part of culture as I define it, but it is the most obvious alternative channel
for any association between culture and results, and it is kept in the analysis for that reason.

Warmth and strictness are not directly observed for schools in England. However, there is public
text available for each school that may be informative about their culture.
Every school has a report written by an external inspector, a behaviour policy written by the
school, and a website written by the school. For a smaller set
of 103 schools this study adds data from direct observation of the school at work and from a 
structured interview with its headteacher.

From these I construct a gold-standard measure of each dimension. Because
the interview and the visit disagree substantially about the same school,
the gold standard is in fact two measures rather than one: an \emph{enacted}
score, from what the observers recorded, and an \emph{espoused} score, from
what the headteacher reported. For a further \NTierTwo{} schools I have the
interview alone, and for all schools nationally we have the public
text.

I then ask how much of the gold standard the public text contains using two methods. 
The first is a prediction model: using the visited schools, a
statistical model learns which words and phrases of a document go with
higher or lower observed scores, and it is tested only on schools it was
not fitted on. Before fitting, identifying details are removed from the text,
and the part of each score that school demographics could explain is
removed from the target, so the model is reading culture from the prose only. The
second method uses a mark scheme approach. A large language model is provided with a one-page written rubric for each dimension and
asked to score each document on a 1 to 5 scale. The first method assumes nothing about where culture 
shows up in text; the second assumes it is there and scores it. Comparing the two methods for each text source and
for each dimension shows not only which measures can be extended to every school in the
country but what method is most appropriate for each dimension.

There are three main findings. The first is that inspection reports
carry a modest but real signal of observed warmth. A prediction model
reading only the report's wording reproduces the observed warmth of
held-out schools at $\PredWOfsted$. The most that any correlation with a score this 
noisy could reach is about $0.83$, a ceiling derived below from the score's reliability. Before intake is removed
from the target the correlation is $\PredWOfstedRaw$, and after it is
$\PredWOfsted$. Removing intake therefore strengthens the signal rather
than creating it, so the signal is carried by the prose itself. The
signal is clearest in what marks the colder schools. Their reports fall
back on stock assurances such as ``pastoral support is in place'' and
``pupils feel confident they have a trusted adult''. The warmer schools'
reports instead spend their sentences describing pupils and adults
dealing with one another, spread across many small choices of phrase
rather than concentrated in quotable lines. Observed teaching behaves
like warmth, with a signal of $\PredTOfsted$.

The second finding is that observed strictness must be read differently.
The prediction model reads it only weakly. The one-page marking scheme reads
strictness at $\RubricSOfstedDec$. That reading is indistinguishable in
size from the best model's. However, it survives a change of language
model and demographic adjustment, and the models' readings do not. The
reason is that the two dimensions are written down differently. Warmth
lives in a report's habitual register, which a model with no
instructions can learn. Strictness lives in specific, checkable claims,
for example ``low-level disruption is rare'', which only directed
questions extract.

The third finding is that the enacted and espoused measures diverge,
and that the divergence is stronger for warmth. The interview and the
visit are correlated at only $\GoldWarmthSplit$ for warmth, against
$\GoldStrictnessSplit$ for strictness. Of the \TopQN{} schools whose
heads rated them in the top quarter for warmth, only \TopQWMatch{} were
in the top quarter as observed. Chance alone would put roughly seven
there, and two noisy readings of the same quantity would put about
\TopQExpectedSameW{}. For strictness the match is \TopQSMatch{} of
\TopQN{}, against a ceiling of about \TopQExpectedSameS{} once
measurement error is allowed for. The same asymmetry runs through the
public text. What schools write in their policies and on their websites
tracks what their heads say about strictness and about staff morale.
However, their warmth language shows no relation to the observers, or even to their own headteacher's responses during the interview. The divergence has an economic reading. What a head says about strictness is checkable, because behaviour leaves a public record: a visitor can watch a corridor, and an inspection can report whether disruption is rare. What a head says about warmth is not, because no record shows whether pupils feel cared for. The economics of communication predicts exactly this split: statements that can be checked can carry information, and statements that cannot be checked need not \citep{milgrom1981, crawford1982}. The schools' words behave as that prediction says.

The paper contributes to various strands of the literature. The school-climate literature measures 
culture through pupil, teacher and parent surveys \citep{hoy2001,cohen2009,thapa2013}. Those surveys have
produced consistent associations between climate and wellbeing, attendance and attainment. 
However, they capture perceptions rather than practice and are open to social desirability. They are also costly at national scale.
\citet{vanhoutte2005} argues that climate, meaning the aggregate of members'
perceptions, should be kept apart from culture, meaning the shared norms that
govern behaviour, and it is culture in that sense that the measures here aim
at. Administrative proxies such as exclusion rates \citep{machin2020}
conflate culture with intake, and the inspection grade folds many dimensions
into a single verdict.

The school-effectiveness tradition that begins with \citet{coleman1966} and
\citet{rutter1979} located the difference schools make in what Rutter called ethos rather than in resources. 
Ethos here means the expectations and routine practice that a school sustains. Later syntheses agreed on a list that includes
an orderly environment and a supportive climate \citep{edmonds1979,sammons1995,teddlie2000}. That strand has stayed largely
qualitative, because ethos has been measured by case study.

Structured classroom observation, in particular the CLASS framework \citep{pianta2008}, measures emotional support and classroom
organisation directly and predicts pupil gains at secondary level
\citep{allen2013} and in English primary schools \citep{sammons2008}. The
observation forms I use draw on those two domains, but they aggregate across
lessons and out-of-lesson settings so as to characterise a school rather
than a teacher. To my knowledge no study has combined such observation with
a leadership interview and public text in order to separate what a school
does from what it says.

The literature on high-expectation, high-structure
charter and academy models
\citep{abdulkadiroglu2011,angrist2010,dobbie2013,fryer2014} finds large
effects of a bundle of practices in which strict behaviour norms and warm,
individual relationships come together. It is the origin of the strictness
dimension and the reason to expect it to matter, but it has not measured the two dimensions apart.

Finally, the text-as-data literature in economics
\citep{gentzkow2019} supplies the tools for reading public text at
scale.  This paper uses both of  that literature's modern tools, supervised text regression and marking schemes applied 
by a large language model \citep{gilardi2023,eloundou2024}. It tests each against direct observation 
and reports which dimension of culture each can read. For one concrete measurement problem, it 
answers the question posed by \citet{ludwig2025} of when instructed language-model
measurement adds value over statistical alternatives.

Against those four literatures, the paper contributes three things: a measure of enacted culture built from 
observation rather than perception, with the perception data from headteachers and parents kept as comparisons 
against it; a model to predict enacted culture in all English state secondary schools; and a like-for-like comparison, on one 
criterion, of the two ways economists now read text, prediction models 
and marking schemes. The comparison shows that which one works is a property of the construct.

The next section describes the data. The paper then constructs the gold standard, describes how the two text readings 
are built and judged, reports what the measures show, and concludes.

% ---------------------------------------------------------------
\section{Data}
\label{sec:p1_data}

The paper uses four kinds of data on English state secondary schools: a structured observation from a visit and a 
headteacher interview that were carried out for this
study, three collections of public text, a national survey of parents, and
the Department for Education's school-level administrative data. The sources divide by whose account of the school they give. 
The visit records what trained observers saw the school do, and the inspection report
records what an inspector saw, so these two are sources on \emph{enacted}
culture. The interview, the behaviour policy and the website record what the
school says about itself, the first to a researcher and the other two in
writing, so these three are sources on \emph{espoused} culture. The parent
survey belongs to neither group. Parents report their own family's experience
of the school, and whether that experience follows what observers see or
what the school says about itself is one of the questions the paper answers.

Together the sources define three tiers of coverage: \NTierOne{} schools with
a visit, an interview and the public text; \NTierTwo{} schools with an
interview and the public text (a handful lack one of the three); and
the rest of the national population
with the public text alone. \Cref{tab:tier_summary} sets out the tiers and
compares the schools in each on intake, size and inspection grade.

\input{tables/tab_tier_summary}

\textbf{Visits and interviews.} Between November 2023 and April 2025 the
research team, working under my direction, interviewed the headteacher at
304 schools and visited \NTierOne{} of them. In eight cases the head was
unavailable and we interviewed the senior leader responsible for behaviour
and culture instead.\footnote{The counts that follow differ by a school or
two from place to place, for four reasons which I state once here. One of
the 304 heads left the statement battery blank, so 303 schools carry an
espoused score and the interview-only tier numbers \NTierTwo{}; that school
belongs to neither tier in \cref{tab:tier_summary}. Of the \NTierOne{}
visited schools, two have no scored inspection report and two have no
published parent survey, so correlations and prediction models that use
those sources run on 101 schools. The deconfounded correlations run on 100, because one school lacks a control variable. 
Among the interviewed schools, between 299 and 302 have each text source. Finally, one visited school and three
interviewed schools could not be matched to the 2023/24 characteristics
panel used in \cref{app:2A:sample}, which therefore describes 102 and 300
schools.} Schools were recruited by approaching headteachers directly by
email and telephone, and by referral from schools that had already taken
part. No quota was applied by region, school type or inspection grade.
Three-quarters of the interviews and half of the visits took place in the
2024/25 academic year. At the end of every interview the headteacher was
asked whether the school would be willing to host a visit, which means that
the visited schools are a self-selected subset of the interviewed ones.

A visit lasted a school day and was made by a team of two, and occasionally
three, researchers. The school was asked in advance to arrange four to six
lessons spread across year groups and subjects, and it chose the particular
lessons itself. The team observed those lessons, typically five of them,
spread across Years 7 to 11. The researchers knew which school they were
visiting but not its inspection grade, and the school knew it was being
observed but not which items were being rated. Each school also allowed us
to observe pupils moving between lessons, and pupils and staff interacting
at break and lunch, which is what the outside-of-lessons form rates. Each observation covered 
either a whole lesson or half of one, and every researcher present rated it independently on the 
lesson observation form. The team also watched arrival to lessons, movement between lessons, break and lunch, and 
at the end of the day each researcher completed the outside-of-lessons form once, summarising the whole day.

The lesson form has twenty items; the mapping from the printed form to the scored items is in \cref{app:2A:guide}. Ten of the 
twenty relate to warmth and strictness, for example the
frequency with which the teacher uses pupils' names, and the level of
misbehaviour tolerated before a first sanction. The other ten relate to
teaching practice, such as questioning, feedback, explanation and structure.
The outside-of-lessons form has thirteen items: nine on the school outside
the classroom (sanctions and rewards, corridors, arrival, the dining hall,
and interactions and relationships at break), two on the condition of wall
displays, and two in which the observer compares the teaching seen across
the day. Every item is rated from 1 to 5 and the item ratings are averaged across researchers and 
across lessons to give one value per school.

The school choosing the lessons is a limitation of the visit as a measure of practice. However, the most likely direction 
of the problem is known. A school will tend to show a visitor its better lessons, so if anything the enacted scores are 
too high and too similar to one another, which makes differences between schools harder rather than easier to find. 
What this argument cannot exclude is differential staging, meaning that some schools are better at performing for a visitor 
than others, and its direction is unknown.

The interview lasted about an hour and followed a fixed guide. It had three
parts: twelve open questions on the school's approach to behaviour,
relationships, wellbeing and teaching, the first of which records how long the headteacher has been in post. 
This was followed by ten statements on which the
headteacher rated their own school on a five-point agreement scale (for
example ``The sanction systems are applied consistently by all staff'' and
``Pupils feel like their teachers care''). The final part was a checklist of ten named behaviour systems (centralised detentions, an
internal exclusion room, a mobile phone ban and so on) recorded as present
or absent. Two researchers attended every interview, one asking the
questions and one observing, and both scored the open questions
independently. The observation forms and the interview guide are reproduced
in full in \cref{app:2A:visit_protocol,app:2A:interview_guide}, and the
agreement between researchers on the observation and interview items is reported together with the measures below.

\textbf{Public text.} Three collections of text cover the national
population. A scored inspection report exists for
3,272 of them (98.2 per cent); the rest had opened too recently to have been
inspected. A behaviour policy document exists for 3,294 (98.9 per cent),
one document per school, taken from the school's website. About one document
in ten is a trust-wide policy adopted word for word by the school. Website
text exists for 3,284 schools (98.6 per cent). This text is taken from the home,
values and welcome pages. The counts differ by a few dozen schools because a
handful of schools have no report, no policy or no website;
\cref{app:2A:sample} gives the reconciliation.

\textbf{Parent survey.} Ofsted's Parent View survey is open to the parents
of every school and is published once enough parents have responded. I use
the most recent release for each school and the two items closest to
warmth: the share of responding parents who agree that their child is happy
at the school, and the share who agree that their child feels safe there.
The two shares are averaged into one parent-reported warmth measure, which
is available for 3,255 schools in the panel and for 101 of the visited
schools. Parents choose whether to respond. The typical release rests on about 120 responses at a response rate of about 12
per cent, so the measure is the view of the parents who chose to give one. Responses also accumulate over several years, 
so for some schools the parent data are older than the culture data. The survey has no item on strictness.

\textbf{Administrative data.} All schools are matched on their unique
reference number to a school-year panel that I built from the Department for
Education's published tables for 2015/16 to 2024/25. The panel carries pupil
composition, prior attainment, Progress~8 (a value added measure) and its components, workforce and
finance. This paper uses the panel only to describe the samples; the
following chapter uses it for outcomes and controls. The panel also carries
each school's most recent overall inspection grade. Inspections from
September 2024 onwards no longer award an overall grade, so the grade used
throughout is the last one each school received before that date, on the
scale 1 (Outstanding) to 4 (Inadequate). Schools inspected only since then,
or never inspected, have no grade and are missing on that variable.

\textbf{Representativeness.} \Cref{tab:2A:representativeness} compares the
visited and interviewed schools with the national population. Both samples
resemble the population on school type, phase, admissions, size and urban
location, and they differ from it on deprivation by at most two percentage
points. Both over-represent London (45 per cent of visited schools against
15 per cent nationally), which shows up as a higher share of pupils with
English as an additional language. Both also lean towards stronger schools:
25 per cent of visited schools held an Outstanding inspection grade as at
August 2024 against 15 per cent nationally, and their mean Progress~8 in
2018/19 was $+0.17$ against $+0.04$. The mismatch on those dimensions is what one would expect from recruiting through headteachers,
since the schools most likely to agree to take part are those that are
confident about what a visitor will find, and it has two consequences. The first is that 
estimates from the visited sample apply most directly to a population that is somewhat stronger than the national average. 
The second is a restricted range: the sample under-represents the weakest schools, so both culture and outcomes 
vary less than nationally.

% ---------------------------------------------------------------
\section{Measurement}
\label{sec:p1_measurement}

This section builds the measures in the order that the design requires. It
first constructs the gold-standard scores from the visits and the interviews
and reports how reliable they are. It then builds the prediction models used to produce scores for the 
schools that were not visited. It ends with the marking schemes read by a large language model, and with how both routes are judged.

\subsection{The gold standard}

The scores are built up in two steps, and they come in two sets that are kept apart. Every observation item 
and every interview statement is rated from 1 to 5. First, items are averaged into
\emph{sub-scores}, each of which covers one dimension in one setting: warmth
in lessons, warmth outside lessons, strictness in lessons, strictness outside
lessons, and so on. Second, the sub-scores for a dimension are averaged into
that dimension's \emph{score}, so that each school ends up with one warmth
score and one strictness score, and each score is doubled from the 1--5 range of the items to a 0--10 scale.

Two sets of scores are constructed and kept apart. The \emph{enacted} scores use the observations alone. Enacted
warmth is the average of in-lesson warmth (the use of names, praise, the
quality of interaction, pupil participation and motivation) and
out-of-lesson warmth (relationships and interactions during transitions and
breaks, and how consistently rewards are used). Enacted strictness is the
average of in-lesson strictness (misbehaviour tolerated, disruption,
response to sanction, respect and concentration) and out-of-lesson
strictness (how consistently sanctions are used, corridors, arrival, the
dining hall and recreational space). Enacted teaching is the average of the
ten lesson items on teaching practice. The \emph{espoused} scores use the
headteacher's statement ratings alone. Espoused warmth is the average of the
three warmth statements (rewards applied consistently; pupils feel safe;
pupils feel their teachers care) and espoused strictness is the average of
the four strictness statements (sanctions applied consistently; behaviour in
lessons; behaviour outside lessons; teachers feel respected). The two
remaining scored statements, on staff turnover and morale, form a
staff-climate score. The item definitions and the mapping of items to
sub-scores are given in \cref{app:2A:guide}.

Not everything that was recorded enters a score. On the visit, the four
outside-of-lessons items left out are the two on wall displays (their
condition, and their alignment with the school's stated values) and the two that compare teaching across the day. 
From the interview, the ratings of how fully the headteacher answered each open question, the
checklist of named behaviour systems and the statement on parental
engagement are left out, and the interview gives no espoused measure of
teaching. The reasons are given below and in full in \cref{app:2A:guide}.

To make the construction concrete, consider the in-lesson strictness
sub-score of a single school. In each of five lessons, two researchers rated
the level of misbehaviour tolerated before a sanction, the frequency of
disruption, the pupils' response to correction, how respectfully they treated
the teacher, and how well they concentrated. Misbehaviour and disruption are
reverse-scored so that a higher value means a stricter lesson. One item
needs a rule. If no pupil was sanctioned during a lesson there is no response
to correction to rate, and the researcher records a zero. That zero is not a
low score, and dropping it would throw away what the lesson shows. It is
therefore replaced by the reverse of the disruption rating, which means that
a lesson with no disruption and no sanction is scored as if the pupils had
responded fully, and a lesson with constant disruption and no sanction as if
they had not responded at all. The five ratings a researcher makes in a
lesson are averaged to give that researcher's value for the lesson, the
lesson values are averaged
across lessons, and the researchers' values are averaged across researchers,
giving one in-lesson strictness figure on the 1--5 scale. That figure is
averaged with the out-of-lesson strictness figure and doubled to give the
school's enacted strictness on the 0--10 scale. Espoused strictness is
simpler: the headteacher's ratings on the four strictness statements are
averaged and doubled. Every school that received a visit has an enacted
score, every school that gave an interview has an espoused score, and
\NTierOne{} schools have both. \Cref{tab:score_dist} reports the distribution
of every sub-score and score across the visited schools.

\input{tables/tab_score_dist}

There are two larger exclusions from the interview. The first is the ratings of how fully the headteacher 
answered each open question, which measure how articulate the head is rather than the culture of the school. The second is 
the count of the named behaviour systems (S3 in the tables), which counts 
eight of the ten and excludes the two reward systems. It correlates with observed strictness at $-0.01$. In other words, 
the systems a school names are not a guide to how strictly it runs.

Agreement between researchers is moderate at the level of a single item, and
the school-level scores built from many items, lessons and raters are
reliable enough to use, as I show below. Of the 517 lessons observed,
\LessonsTwoRaters{} were rated independently by two researchers and
\LessonsThreeRaters{} by three; in the remaining \LessonsOneRater{} only one
researcher was available for that lesson, and it carries one rating. The
outside-of-lessons form was completed by two or more researchers for every one of the \NTierOne{} visits, though individual items
were left blank when not observed, so the item-level comparisons below
rest on fewer days than that. Where two raters saw the
same lesson, the pairwise Pearson correlations across the classroom items
range from $0.53$ to $0.79$; on the outside-of-lessons form, completed once
per day, they range from $0.47$ to $0.81$; and the two raters are within
one point of each other close to 90 per cent of the time
(\cref{tab:irr_classroom,tab:irr_outside}). Agreement is highest on the
items with the most concrete anchors, namely the consistency of the reward
system ($0.81$, a day item) and the frequency of disruption ($0.79$), and
lowest among the classroom items on the teaching-practice ratings that ask
the observer for the most interpretation, such as the use of resources and
the choice of methods ($0.53$ and $0.55$). 

However, the paper's claims do not rest on the agreement of two raters on
one lesson. They rest on the reliability of a school's score once it has
been averaged over its lessons and raters. \Cref{tab:score_reliability} gives this. Take every rating that a
researcher gave a lesson for in-lesson strictness: one number, for one
lesson, from one researcher. Those ratings differ from one another for three
reasons. The first is that the lesson was in a stricter or a less strict
school, which is the difference the measure is meant to capture. The second
is that, within the same school, that lesson was calmer or rowdier than the
school's other lessons, because it had a different teacher, a different
class or a different period of the day. The third is that, looking at the
same lesson, one researcher scored it a little higher than the other. The
table divides the spread of the ratings into those three shares. For
in-lesson strictness the shares are \ShareSchoolS{} per cent school,
\ShareLessonS{} per cent lesson and \ShareRaterS{} per cent rater. This means
that well over half of the variation in single-lesson strictness ratings is
lessons within the same school differing from one another, only about a
fifth is schools differing from each other, and under a fifth is the two
researchers disagreeing. For in-lesson warmth the shares are
\ShareSchoolW{}, \ShareLessonW{} and \ShareRaterW{} per cent. Lessons within
a school vary less in warmth than in strictness, but the researchers
disagree more about warmth, which matches the item-level pattern described
above.

\input{tables/tab_score_reliability}

This decomposition has two implications. The first is that
averaging matters. A single lesson is a poor guide to its school, but when
five lessons and the ratings of the researchers who saw them are averaged,
the lesson-to-lesson and rater-to-rater variation is greatly reduced. The reliability of the in-lesson warmth score, 
averaged over a school's own lessons and raters, is \RelWLessons{}, and that of the in-lesson 
strictness score is \RelSLessons{}. The outside-of-lessons sub-scores, which summarise a whole day, are more
reliable (\RelWOutside{} and \RelSOutside{}). The in-lesson and out-of-lesson halves of
each enacted score, which were rated in different settings, are correlated
at \HalvesW{} for warmth and \HalvesS{} for strictness across the \NHalves{} schools. This puts the 
reliability of each combined enacted score at about \RelEnactedW{} for warmth and \RelEnactedS{} for strictness. 
The components are computed before rounding. The ceiling on any correlation with the enacted warmth score is the 
square root of its reliability, $\sqrt{0.69} \approx 0.83$. These
figures treat the two settings as parallel forms of one measure; treating
them instead as distinct facets of the construct gives \RelEnactedWUpper{}
and \RelEnactedSUpper{}. The corrections below use the lower figures, so
the corrected correlations are upper bounds. Each school was visited once, so day-to-day variation within a school
cannot be separated from the school itself: these are reliabilities of the
school as seen on its visit day.

The lessons that carry only one rating cost little here. Had every lesson been rated twice, the in-lesson
reliabilities would have been \RelWOneTwoRaters{} and \RelSOneTwoRaters{}
rather than \RelWLessons{} and \RelSLessons{}, because the noise that averaging has to
remove is mostly between lessons and not between raters. They do, however,
carry one caution. Single-rated lessons were rated lower on average than double-rated ones, by \SingleRatedGapW{} on warmth 
and \SingleRatedGapS{} on strictness on the 1--5 scale. The \SchoolsWithSingleRated{} schools with such lessons have 
slightly lower scores as a result: the share of a school's single-rated lessons correlates with 
its in-lesson warmth at $r = \CorrSingleShareW{}$. The following chapter therefore checks that its
estimates do not depend on those schools.

The second implication concerns schools themselves rather than the measurement. Behaviour varies more from classroom 
to classroom within a school than it does between schools. A school's enacted score is therefore an average over 
quite different lessons, and a visit of five lessons measures that average with error. The error pulls every correlation 
with the enacted scores towards zero, so the figures reported below are, if anything, too small. Two corrections give a 
sense of how much. The enacted--espoused correlations, corrected for the noise on both sides, rise to 
about \DisSplitW{} for warmth and \DisSplitS{} for strictness, which is still far short of what two readings of the same 
school would give. The marking-scheme strictness correlation of $\RubricSOfstedRaw$, corrected for the enacted score's noise 
alone, would be about $0.36$. Neither correction is large enough to change a conclusion, and the uncorrected figures are the ones 
reported throughout.

The two enacted scores are correlated at $r = \CorrWarmthStrictnessEnacted$ across the \NTierOne{} visited schools, and the espoused 
pair at $\CorrWarmthStrictnessEspoused$. Read at face value, the enacted figure says that warm schools are usually strict ones. 
It should not be read that way, because part of it is produced by the measurement itself: the same observers scored both dimensions 
at the same school on the same day, and a good impression lifts both ratings at once. The lessons that two researchers rated 
independently show how much this explains. If the correlation lived inside each observer's head, an observer's warmth rating would 
predict their own strictness rating much better than their colleague's. It barely does: \CorrWSSameObs{} against \CorrWSCrossObs{}. 
So the inflation comes from the shared visit rather than from individual judgement, since both observers watched the same lessons at 
the same school on the same day. Removing measurement noise from the cross-observer figure puts the correlation between the dimensions 
themselves at about \CorrWSTrue{}. That, rather than $\CorrWarmthStrictnessEnacted$, is the better estimate of how strongly warmth and 
strictness go together in schools, and it says the two are related but clearly distinct.

The gold standard is two measures rather than one because the interview and the visit disagree about 
the same school. If the two were noisy readings of one quantity, the right thing to do would be to average them, 
because their errors would partly cancel. However, they agree far too little for that: the two warmth measures share about a 
sixteenth of their variance, and the two strictness measures about a sixth. Averaging two measures of different things does not 
cancel noise. It produces a third quantity that is neither of them, and that changes with the weights chosen. I therefore keep 
the two measures separate throughout, and their divergence is itself a result, reported with the results. Strictness is the 
borderline case: its corrected correlation reaches \DisSplitS{}, so there the two measures partly overlap rather than measure 
different things outright. Two schools show what the divergence looks
like in the ratings. At the first, the headteacher rated the school 8.0 on
warmth and 8.0 on strictness, and the observers, across their lessons and
the day, recorded 7.9 and 8.5; the in-lesson and out-of-lesson sub-scores
agree with one another to within half a point. At the second, the
headteacher rated warmth 9.3 and strictness 7.8, near the top of the scale,
while the observers recorded 4.6 and 5.9. In-lesson warmth at that school
averaged 2.2 out of 5 and out-of-lesson warmth 2.4, while the head's ratings
on the three warmth statements averaged 4.7 out of 5. There is nothing in
the interview that would tell these two heads apart. Across the \NTierOne{} schools, four
in five have a warmth gap larger than one point on the 0--10 scale and half
have a gap larger than two.

One espoused measure can be checked against an outside record. Heads rated how low their staff turnover was and how high
morale was, and the staff-climate score built from those two statements is
correlated with the school's actual teacher turnover, as recorded by the
Department for Education, at $r = -0.52$, and at $-0.48$ once the other
espoused scores, disadvantage and school size are held constant. On the one
question where a head's answer can be checked against the record, heads
report accurately. This check concerns staff turnover, though, and says nothing about the warmth statements.

\subsection{Reading the text with prediction models}
\label{sec:p1_prediction}

The first way of reading the documents uses no theory of culture at all. It asks only whether the words a document uses tend to 
go with the culture observers found in the school.

The method works as follows using warmth as an example. Each document is turned into a list of counts:
how often each word, and each two-word phrase, appears in it. Phrases that
appear in almost every document count for little; phrases that distinguish
one document from another count for more. A statistical model is then
fitted on the visited schools. It learns which phrases tend to appear in
the reports of schools observed to be warm, and which in the reports of
schools observed to be cold, and it attaches a small positive or negative
weight to each. To score a new school, the model adds up the weights of the
phrases in its document. No single phrase decides anything: a school's
score is thousands of tiny nudges summed. (\cref{sec:p1_app_prediction} gives the specification.)

Three safeguards stop the model from scoring well for the wrong reasons.

First, the model is never tested on a school it learned from. Every school's reported prediction comes from a model fitted 
with that school left out, so each number says how well it reads a school it has never seen. As a further check, I re-ran the 
whole procedure two hundred times on deliberately scrambled scores. Scrambling leaves nothing real to find, so whatever correlation 
the model still reaches is what luck alone produces. A real result has to beat that, and the reported significance levels say how 
often scrambling did.

Second, it cannot recognise the school. Before anything is counted, school
and trust names, place names, dates, page numbers and standard boilerplate
are removed from every document. Without this step, the model learns to spot which school or which era of report it is reading, 
rather than what the report says. With it, the weights fall on ordinary prose.

Third, it gets no credit for reading demographics. A school's intake
statistics predict part of its culture score, and a document often reveals
the intake. So each observed score is first split into the part that
intake statistics explain and the part they do not, and the model is asked
to predict only the second part. A model that merely recognised a
disadvantaged school from its report would score nothing. Results are
shown with and without this adjustment.

I also tried more elaborate ways of representing the text, and none beat simple counting: longer phrases, 
topic groupings, and meaning-vectors from a neural network that scores passages as similar when they say similar 
things in different words. \cref{sec:p1_app_prediction} records them. The counting representation is the one reported throughout.

\subsection{Reading the text with marking schemes}
\label{sec:p1_rubrics}

The second way of reading the documents states what to look for. For each
source and dimension I wrote a marking scheme of one page: a sentence
saying what is being measured, five numbered bands, and two or three
tie-break rules. To give the flavour, the inspection-report warmth scheme
sets band~3, the default, as ``a safe and generally positive school,
described through labels, provision or outcomes, without describing how
staff and pupils actually relate'', and band~4 as ``the report describes
the relationships themselves''. All seven schemes are printed in full in \cref{sec:p1_app_rubrics}.

In the results, each marking scheme is judged against the visit scores, one source and one dimension at a time, and one 
deliberate difference between the two sides matters for those comparisons. On the visit, consistently used rewards count as warmth, 
because a reward given in person is a relational act: an adult notices and affirms a child. In the policy scheme, rewards do not 
count as warmth, because a policy describing a reward system is provision on paper rather than a relationship observed. Consistent 
sanctions count as strictness on both sides, because consistent enforcement is what makes conduct rules real, whether it is watched 
or written down.

A language model reads each document against the scheme and returns a band from 1 to 5, together with one quoted sentence as 
evidence (the models and their pinned versions are given in \cref{sec:p1_app_rubrics}). The same fixed model reads every document. Here 
the benchmark is what observers recorded in the school.

Three checks accompany the scores, and their outcomes are reported with the results. The first asks whether the language model gives the same answer twice: thirty documents per source were scored a second time, and the agreement between the two runs is reported. The second asks whether the readings depend on which language model did the reading: the whole exercise was repeated with a second, larger model. The second model's job was fixed in advance, and it is narrow. Its readings are never reported as results. They settle a single question: when the prediction model and the marking scheme tie for a dimension, the tie goes to the one whose reading holds up when the model changes.

The third asks whether the schemes are reading length rather than culture. A longer document gives the model more evidence to cite, so a scheme could award higher bands to longer documents rather than to warmer or stricter schools. The correlation between band and document length is therefore computed for every source and dimension, and each scheme correlation is recomputed with the effect of length removed.

\subsection{Judging both methods}
\label{sec:p1_judging}

Both methods are judged in the same way: a method is good if its scores agree with what trained observers recorded in the school, 
and it earns no credit for anything else. Scores that look sensible, or that agree with other measures, count for nothing on their own. 
Comparing the two methods source by source and dimension by dimension then answers two questions at once. The first is how much 
the public text says about a school's culture at all. The second is what it takes to read it: whether the words must be 
interpreted against a stated theory, as the marking schemes do, or whether counting them is enough, as the prediction models do.

One further rule applies throughout. Warmth, strictness and teaching are different things, so nothing about a document 
should help predict all three at once. Anything that does is reading the document rather than the school, and it is discarded. 
Document age, format and length were all discarded for this reason, and so were several apparent improvements to 
the models (\cref{sec:p1_app_prediction}).

\section{Results}
\label{sec:p1_results}

The results come in three parts: what direct measurement shows on its own, what the public text carries and which 
method extracts it, and two checks from outside the project's own data.

\subsection{What direct measurement shows}

A school can be warm without being orderly, and orderly without being warm, and the visited sample contains 
both kinds (\cref{fig:p1_quadrants}). Warmth and strictness are related but distinct: once observer effects and measurement 
noise are removed, the two correlate at \CorrWSTrue{}. The enacted scores are reliable enough to use, and their limits are 
measured rather than assumed; \cref{tab:score_reliability} reports the reliabilities.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{fig_quadrants_ch2.pdf}
  \caption{Observed warmth and strictness in the visited schools. Each
  dot is a school. Schools are found in all four quadrants: warm-and-strict, warm-and-permissive, cold-and-strict, and cold-and-permissive. The evidence that the dimensions are distinct is the cross-observer trait correlation of about $0.36$ (Section 3) rather than the scatter's spread, which noise alone would produce.}
  \label{fig:p1_quadrants}
\end{figure}

What heads say and what observers see are different measures. The interview and the visit are correlated at only $\GoldWarmthSplit$ 
for warmth ($p = \GoldWarmthSplitP$) and $\GoldStrictnessSplit$ for strictness ($p \GoldStrictnessSplitP$), on the \NTierOne{} schools
with both.

A simple count shows the gap. The heads' ratings put \TopQN{} schools in the top quarter for warmth. The observers' scores also 
have a top quarter. If the two measures agreed perfectly, the same \TopQN{} schools would be in both. In fact only \TopQWMatch{} of
them are.

Two comparisons say whether \TopQWMatch{} is a lot or a little. Suppose first that the heads' ratings had nothing to do with the 
observers' at all. A quarter of the heads' top schools would still land in the observers' top quarter by luck, which is six or seven 
of the \TopQN{}. Now suppose instead that heads and observers were measuring exactly the same thing, each with some error. The two 
lists would still not match perfectly, because the error scrambles each list a little. Given how noisy the two measures are, the
 match would be about \TopQExpectedSameW{} of the \TopQN{}. The actual match of \TopQWMatch{} sits at the first benchmark, not the 
 second: heads' warmth ratings agree with the observers no better than luck would produce. The heads' scale is coarse, so ties can 
 be broken either way. Breaking them the other way gives 9 rather than \TopQWMatch{}, and the conclusion is the same.

Strictness is different. There the match is \TopQSMatch{} of \TopQN{}, and the most that two noisy measures of the same thing 
could manage is about \TopQExpectedSameS{}. Heads place their school's strictness almost as accurately as the measures allow. 
Their warmth placements are the anomaly, and noise does not explain them: correcting the warmth correlation for the noise in 
both measures raises it only to $0.42$.

\subsection{What the public text contains}

\input{tables/tab_p1_grid}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{fig_ch2_validation.pdf}
  \caption{The warmth model and the strictness scheme against the enacted scores, on the deconfounded scale (the part of each enacted score that the four adjustment variables do not explain). Left: the warmth prediction
  model's held-out predictions against observed warmth (each school
  predicted by a model that never saw it). Right: the strictness
  marking scheme's bands against observed strictness.}
  \label{fig:p1_validation}
\end{figure}

\Cref{fig:p1_validation} plots the warmth scores from the prediction model and the strictness scores 
from the marking scheme against the enacted scores: the warmth model's held-out predictions on the 
left, the strictness scheme's bands on the right. In both panels the relationship is loose but 
not the work of a few outlying schools. 

The grid (\cref{tab:p1_grid}) reports fourteen readings of the public text: seven pairings of a 
source with a dimension, each read twice, once by the prediction model and once by the marking scheme. Each reading then faces two tests: it must beat luck, and it must stay significant once the number of readings is allowed for. Every reading that passes both tests comes from an inspection report, and none of the readings of the documents schools write about themselves passes either. Within the 
reports, warmth and strictness come out by different routes.

The scrambled scores say how easily each of the model's readings could be luck. Its three 
inspection-report readings all beat luck (warmth \PermPOfstedW{}, teaching \PermPOfstedT{}, 
strictness \PermPOfstedS{}). Its readings of the behaviour policies and the websites all fail to.

Fourteen readings also give luck fourteen chances, so the grid is corrected for the number of 
tests as well. Four readings pass this test: the model's warmth and teaching, and the scheme's 
strictness and teaching (the strictness scheme at $q = \QGridSOfsted$). 

The prediction model's surviving readings are warmth and teaching. It reads observed warmth from a 
report's wording at $\PredWOfsted$ and observed teaching at $\PredTOfsted$, and both figures are essentially 
unchanged, or higher, when everything demographics could explain is removed. Strictness is 
different: no prediction model reads it well. The best attempt reaches $\PredSOfsted$, it beats 
luck only narrowly (\PermPOfstedS{}), and its confidence interval touches zero ($\CIPredSOfsted$). 
Every word-count representation tried showed the same weakness.

For warmth and teaching the prediction model was the better reader. For strictness the marking scheme's number is the higher one, although the gap between the two is small enough to be chance at this sample size ($z = \StrictDiffZ$, $p = \StrictDiffP$). The numbers alone therefore cannot separate the two methods on strictness. The three checks set out in the measurement section can, and the scheme's reading passes all three.

The retest asks whether the language model gives the same answer twice. It does: across all sources the two runs give the same band \GridRetestExact{} of the time, the inspection-report and policy strictness schemes agree with themselves at 100\%, and the one weak spot is the policy warmth scheme at 83\%.

The change of language model asks whether the reading belongs to the scheme or to the particular model that applied it. If the scheme is doing the work, a different model applying the same page should find the same thing. For strictness it does: the second model reads the enacted scores at $\RubricSFourO$, close to the primary model's $\RubricSOfstedRaw$. For warmth and teaching it finds essentially nothing ($\RubricWFourO$, $\RubricTFourO$). Only the strictness reading transfers.

The length check asks whether the bands track document length rather than the school. For the inspection-report schemes the worry is small: the strictness bands correlate with length at $\LenOfstedS$ and the warmth bands at $\LenOfstedW$, and removing the effect of length moves the strictness reading only from $+0.25$ to $\RubricSOfstedLenP$. Length does not carry the result. The website schemes are different: their bands track length at $\LenWebS$ and $\LenWebW$, and even with length removed ($\RubricWWebLenP$, $\RubricSWebLenP$) the website readings stay in doubt.

The strictness reading also survives demographic adjustment ($\RubricSOfstedDec$). The model's strictness reading, already marginal, has nothing similar in its favour.

The one-page strictness scheme scores $\RubricSOfstedRaw$ raw and $\RubricSOfstedDec$ with demographics removed. The warmth scheme fails entirely: it reads the reports at $\RubricWOfstedDec$ and puts three-quarters of schools in one band. The model reads the same reports at $\PredWOfsted$, and its advantage is itself significant (paired test, $p = \WarmDiffP$).

The two dimensions are therefore written down differently. Warmth shows
in a report's everyday register, spread across many small choices of
phrase, which a model with no instructions can learn but a marking scheme
cannot pin down. Strictness shows in specific, checkable claims, for example whether 
disruption is rare or whether rules are applied consistently, which have to be asked for, 
and which the prediction model cannot distinguish from their negations. Teaching, kept in the 
analysis as the rival explanation for the next chapter, sits in between and is read about 
equally well by both routes in the primary run. However, only the model's reading survives 
the change of language model (the second model's teaching scheme reads $\RubricTFourO$), so 
teaching stays with the prediction model.

Behaviour policies and websites tell neither method anything about enacted culture. The prediction 
models reach at most $\OwnVoiceMax$ on any pairing, too little to distinguish from zero at this 
sample size, and the marking schemes find less. One of those two failures needs a caution. The 
two schemes that read the behaviour policies, one for warmth and one for strictness, each put 
nearly every school in the same band, and a scheme that gives nearly every school the same score 
cannot correlate with anything, whatever the policies contain. Its zero is therefore not evidence 
that the policies are empty. The evidence is that the prediction models, which do spread schools 
out, also find nothing. The two methods never disagree about a source.

\subsection{What the models read}

A model can score well for two reasons: because it has learned the dimension it was trained on, or 
because it has learned the general tone of a school doing well. The mix of the two can be measured. 
Every sentence in these reports carries an independent topic tag saying what it is about 
(relationships, behaviour, teaching, or something else). Take the hundred sentences the warmth 
model rewards most. \WarmTopShare{} of them are about staff--pupil relationships, against a base 
rate of \WarmBaseShare{} across all sentences, so the model seeks out relational content at well 
above chance. For teaching the corresponding shares are \TeachTopShare{} against \TeachBaseShare{}, 
which is no enrichment at all. That is not a failure. Teaching is what inspection reports mostly 
discuss anyway, so the teaching model reads the report's broad register rather than any one topic. 
Each model's signal is therefore partly language specific to its dimension and partly the register 
of a school doing well. The deconfounding step removes the part of that register that the four 
adjustment variables (three intake measures and report age) can explain. The scores that remain 
should still be read as reading the report's whole account of the school, weighted toward the 
dimension.

\Cref{tab:p1_examples} shows the sentences each model weighs most heavily, under selection rules 
stated with the table. The two listings differ, and the difference echoes the chapter's central 
contrast.

For teaching, the strongest sentences are about pedagogy: choosing core knowledge, embedding learning in long-term memory, pupils focusing in lessons. Some of the strongest, though, are criticisms. A sentence saying that pupils are not remembering knowledge deeply raises the score as surely as one saying that they are. That happens for two reasons, one a flaw in the model and one a fact about reports. The flaw is that the model counts words and cannot tell praise from criticism. The fact is that an inspector only mentions long-term memory at all when the school teaches in that style, so even a criticism in this vocabulary picks out a school that works that way (the appendix discusses this).

For warmth the pattern is different, and at first sight stranger. The sentences that most lower a school's warmth score sound kind. ``Many pupils feel confident they have a `trusted adult' they can go to'' is among the strongest score-lowering sentences in the corpus, and phrases like ``pastoral support is in place'' sit beside it. In the model's reading, reassurance of this kind marks the colder school. The reading makes sense once the sentences are seen from the inspector's side. An inspector writes about a warm school by describing adults and pupils dealing with one another. When there is nothing like that to describe, the inspector certifies the support systems instead. The assurance is what gets written when the relationships are missing.

On the positive side, no single sentence carries much weight. The model's warmth evidence is spread across many small choices of phrase: attentive verbs, pupils' names used, care described in passing. Even its strongest whole sentences read as the tone of an engaged community rather than tenderness. The spread is a fact about warmth, not a weakness of the model. Warmth never concentrates in one sentence, so there is no quotable line to point to. The case that the model is reading warmth therefore rests on two things shown already. The first is the held-out correlation: the model predicts the observed warmth of schools it never saw, at $\PredWOfsted$. The second is the topic shares: the sentences the model rewards most are about staff--pupil relationships far more often than their share of all sentences would predict. \Cref{sec:p1_app_examples} also lists the sentences in which the model rewards warmth towards staff: schools whose leaders listen to their teachers score higher. That part of the signal is easy to interpret, though it sits one step away from the classroom.

\input{tables/tab_p1_examples}

\subsection{The school's own voice}

The documents a school writes about itself reveal what its head says, not what the school does. The test runs the prediction exercise again with a new target. As before, the models read the policy and website text; this time they predict what the head said in the interview, not what the observers saw, across the roughly 300 interviewed schools. On strictness the documents echo the head: the models recover the head's account at $\PredEspS$ (demographics removed). On staff morale they echo the head at $\PredEspClim$. On warmth they do not echo the head at all: the models reach $\PredEspW$, which is not significant at the 5 per cent level. What a school writes about warmth is unrelated even to what its own head says in private.

One caveat qualifies that last result. The espoused warmth score is built from just three interview statements, and heads bunch their answers near the top of the scale, so the score varies little from school to school. A target that hardly varies understates any relation with it. Finding nothing against such a target therefore rules out a strong link between public warmth language and the head's account, but not a modest one.

The inspection report is the mirror image. Its text relates to the head's account of strictness at $\CrossWireRaw$, which looks at first like knowledge of what the head thinks. Much of it is not. The report describes what the school does, and what the school does is itself related to what the head says. Hold the observers' strictness score fixed, and the report's link to the head falls from $\CrossWireRaw$ to $\CrossWireNet$. The report knows the school, and through the school it seems to know the head. Each kind of document reflects its own side of the espoused--enacted divide.

\subsection{A check against parents}
\label{sec:p1_pv}

The last check comes from parents. The measure is the parent-reported warmth score built from Ofsted's Parent View survey: the share of parents who say their child is happy at the school and feels safe there. Two caveats travel with it. The first is who answers. Parents choose whether to respond, and the typical school's figure rests on about 120 replies, roughly one parent in eight, so the measure is the view of the parents who chose to give one. The second is when they answered. Responses pile up over several years, so at some schools the parents' answers describe an earlier version of the school than the one the culture data describe.

The main test uses the prediction model at national scale. The warmth model was fitted on the visited schools alone, about a hundred of them. That leaves \PVPredWarmthN{} schools the model has never seen. For each of these, the model reads the school's inspection report and turns it into a predicted warmth score. Those predictions can then be set against what parents say. Across the \PVPredWarmthN{} schools, predicted warmth and parent-reported warmth correlate at $\PVPredWarmth$ (\cref{fig:p1_pv}). Nothing circular can be behind that agreement. The model never saw these schools when it was fitted, and it never saw any parent data at all. It connects to the parents' answers only through whatever both know about the school.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.75\textwidth]{fig_ch2_pv.pdf}
  \caption{Predicted warmth and parent-reported warmth, nationally
  ($r = \PVPredWarmth$, $n = \PVPredWarmthN$ schools). Each dot is
  a bin of schools (twenty equal-count bins); the model never saw
  any of these schools in training.}
  \label{fig:p1_pv}
\end{figure}

The visited schools complicate the picture. There the parent measure can be set against both accounts of the school at once: what the observers saw and what the head said. Against observed warmth it correlates at $+0.17$, positive but too imprecise to lean on ($p = 0.08$, $n = 101$). Against the head's espoused warmth it correlates at $+0.43$. Parents agree with the head more than with the observers. The likely reason is tone. Satisfied parents and self-assured heads both describe the school warmly; observers score what they watched. The survey therefore cannot judge between what schools say and what observers see. What it can do is corroborate the national text signal.

A further check asks whether the model is simply reading the inspection grade, the free one-number summary every school already has. If the grade were all the model reads, then comparing schools with the same grade should destroy the correlation. It barely moves it. Restricting to the 99 schools with a pre-COVID grade leaves the held-out warmth correlation at $+0.32$, essentially its full-sample value of $+0.33$, and comparing schools with the same grade moves it only to $+0.29$. Teaching behaves the same way. The model is not reading back the school's old grade.

There is a harsher version of the test: comparing schools graded alike by the very report the text comes from. That halves the warmth correlation, to $+0.16$. It proves less than it seems to, though. The prose and the grade are two outputs of the same inspection, so this comparison throws away part of what the inspection itself concluded, not just a rival explanation. The national parent correlation also falls by about half when the grade is held constant, from $+0.29$ to $+0.14$. For scale, the grade alone is a weak guide to warmth: on the visited schools it correlates with observed warmth at only $-0.20$ (grades run from 1, Outstanding, to 4, so a negative sign means better-graded schools are warmer). Whatever the model is reading, it is more than the grade could supply.

One caveat needed testing. Inspectors read Parent View during inspections, and about 3 per cent of reports quote it, so the model could in principle be reading the parents' own answers back to them. Deleting every sentence that mentions the survey and re-running the whole prediction leaves the correlation unchanged at $+0.29$. The parents' answers are not smuggled in through the text.

Beyond that, the two measures have nothing in common that could manufacture agreement. Parents answer from their child's daily experience. The model reads an inspector's prose. No shared method, no shared text, no shared respondent connects them, so when two sources this different agree, the agreement has to come from the school itself. One reservation remains. A school that is good in general tends to have both a warmer-reading report and happier parents, so part of the agreement may reflect general quality rather than warmth in particular. The agreement therefore stands as corroboration rather than proof. Schools whose inspection reports read warmer are schools whose parents report happier, safer children.


\section{Conclusion}
\label{sec:p1_conclusion}

Warmth and strictness can both be measured by observation and in public text. Warmth is the harder 
of the two to catch. In person it takes more lessons and more settings to 
pin down than strictness does. In public text it can be read only by counting. A prediction model 
finds it spread thinly across an inspection report's everyday wording, in whether the inspector 
writes about adults attending to pupils or falls back on stock assurances. A marking scheme cannot 
see it at all, because no single sentence carries it.

Strictness is the opposite. In person it is easy to observe reliably. In text it sits in specific claims: whether disruption is rare, whether rules are enforced consistently. Claims defeat word counting, because a report saying that disruption is rare and one saying that it is common use almost the same words. That is why the models read strictness weakly and why their readings did not stand up to the checks. The same property is what the marking scheme exploits. A claim can be asked about: the scheme asks whether the report says conduct is orderly, and the sentence either says so or it does not.

The documents schools write about themselves track what heads say about strictness and staff 
morale. On warmth they carry no signal detectable at this sample size, either against the observers or against what headteachers say when interviewed. The split is the one the economics of communication predicts: the claims that can be checked are the ones that carry information. 

Parents are the one outside source. At national scale their reports of happy, safe children agree with the model, which supports the claim that inspection reports carry a real warmth signal. On the visited schools they agree with the head more than with the observers, so where those two accounts of a school disagree, parents cannot say which one describes it accurately.

The practical product is two national measures, each validated against direct observation: 
a prediction model for warmth (and teaching), and a one-page marking scheme for strictness. 


There are four limitations. The first is that the visit's own reliability caps every correlation reported here well below one, so the true relations are likely stronger than the reported ones. The second is that inspection reports are written in different years under different frameworks, which adds noise 
that cleaning reduces but cannot remove; noise of this kind pushes correlations towards zero, 
not away from it.

The third is that each school was visited once. A visit is a 
snapshot, and a school may have had an unusually calm or an unusually rough day; the scores cannot 
tell an unusual day from an ordinary one. The final limitation is that the model's warmth reading cannot be fully separated from the inspection's overall verdict. The prose the model reads and the grade the inspection awarded come from the same visit and say related things, so part of what the model reads may be the inspection's judgement of the school rather than its warmth in particular. The checks in the results show that the model reads more than the grade alone. What they cannot show is how much of the rest is warmth, because no comparison can strip out the inspector's verdict without stripping out some of the warmth with it.

\startchapterappendix{2}
\thispagestyle{plain}
\vspace*{6\baselineskip}
\begin{center}
  {\Huge\bfseries Appendix to Chapter 2}\\[10pt]
  {\large\itshape Measuring warmth and strictness}\\[14pt]
  \rule{0.45\linewidth}{0.6pt}
\end{center}
\vspace{2\baselineskip}
\begin{center}
\begin{minipage}{0.82\linewidth}
\noindent This appendix collects the supporting material for the chapter,
in the order in which the chapter uses it. It sets out how the visit and
interview items combine into scores, reproduces the two observation forms and
the interview guide as they were administered, gives the inter-rater
reliability tables, describes the samples and the coverage of the text
collections, specifies the prediction models, lists the sentences and phrases those models weigh most heavily, and finally reproduces the
text of every marking scheme given to the language model.
\end{minipage}
\end{center}
\clearpage

\clearpage
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\section{Scoring Guide and Item Definitions}
\label{app:2A:guide}

This section sets out how the individual items on the forms combine into
the nine sub-scores and the two sets of scores. The items themselves, that
is, the question wording, the response format and the anchor description
attached to each rating level, are reproduced in full in
\cref{app:2A:interview_guide} for the interview and in
\cref{app:2A:visit_protocol} for the two observation forms.

Every item is recorded on a 1--5 scale. Two observation items can also take
a zero, with a distinct meaning in each case. A zero on \emph{response of
pupils to sanctions} records that no sanction was issued during the lesson,
and a zero on \emph{effectiveness of pupil discussion} records that no
discussion was initiated. For the reasons given in the chapter, the first of these is not a genuine low score. It is replaced by $6 - \text{disruption}$, which gives a calm unsanctioned lesson a high value
and a disrupted unsanctioned lesson a low one. The second is treated as a
genuine negative, since a lesson containing no discussion did not achieve
effective discussion.

Observation item scores are averaged across lessons and across researchers
to give one value per school before the sub-scores are formed.
\Cref{tab:p1_subscore_items} lists the constituent items of each sub-score.

\input{snippets/subscore_items}

\paragraph{Items recorded but not scored.} Every item on the lesson
observation form enters a sub-score. Four items on the outside-of-lessons
form do not. Two of them, the consistency of verbal feedback and the quality
of differentiation across lessons, are recorded as cross-lesson summaries of
the corresponding lesson observation items, and so they enter no sub-score
of their own, because the lesson items already capture them. The other two,
the condition of wall and corridor displays and the extent to which displays
align with the school's stated values, are ratings of the building rather
than of conduct or relationships, and they fit neither dimension as it is
defined. Both are retained in the data.

From the interview, three components are recorded but not scored. The first is the set of ratings of how fully the headteacher answered each open question (Q5 to Q8, Q11 and Q12). As the chapter reports, these measure how articulate the head is rather than how the school runs, and they enter no score. The second is the checklist of ten named behaviour systems, counted as S3. Its near-zero correlation with observed strictness, reported in the chapter, is why it enters no espoused score. Of the ten systems, S3 counts the eight that
represent behavioural or academic requirements and omits the two reward
systems (reward assemblies and reward points), whose presence indicates a
warmer rather than a stricter regime, and it is the count of those eight
rescaled to 0--5. The third is the tenth statement, on parental engagement,
which belongs to neither dimension. Two further statements, on staff turnover
and morale, are scored but form the staff-climate score SC rather than a culture score. The remaining
questions (Q1 to Q4, Q9 and Q10, which cover length of tenure, the
description of the school's culture and how it is communicated, the
characteristics of an effective school, the staff structure and
accountability) were asked for context, and their ratings, where they were
taken, enter no score.

Every other sub-score is the mean of its items, computed over the items
present so that a missing item reduces the number of contributing items
rather than the school's score.

The sub-scores combine into two sets of scores, grouped by source, with
observed behaviour and stated philosophy kept apart:
%
\begin{align}
\text{Enacted:}\quad
  W^{\text{enac}} &= \overline{(W1, W2)}, &
  S^{\text{enac}} &= \overline{(S1, S2)}, &
  T^{\text{enac}} &= T1 \label{eq:enacted} \\
\text{Espoused:}\quad
  W^{\text{esp}}  &= W3, &
  S^{\text{esp}}  &= S4, &
  \mathrm{SC} &= \overline{(\text{SC}_{1}, \text{SC}_{2})} \label{eq:espoused}
\end{align}
%
There is no espoused teaching score. The two interview statements under the
teaching heading concern staff turnover and morale, and they form the staff
climate score SC instead. The systems count $S3$ enters no espoused score.

Schools with an interview but no visit have the espoused set of scores alone
(every visited school was also interviewed). No imputation is attempted across the two sets: as the chapter reports, the espoused and enacted scores are too weakly correlated for either to stand in for the other. All
scores are finally doubled from the 1--5 item scale to the 0--10 range.

The mapping in \cref{tab:p1_subscore_items} is generated directly from the
scoring code, so it cannot drift from what the scoring code does.

\clearpage
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\section{School Visit Protocol}
\label{app:2A:visit_protocol}

\input{snippets/visit_protocol}

\clearpage
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\section{Interview Guide}
\label{app:2A:interview_guide}

\input{snippets/interview_guide}

\clearpage
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\section{Inter-rater Reliability Tables}
\label{app:2A:irr}

\input{tables/tab_irr_interview}

\input{tables/tab_irr_classroom}

\input{tables/tab_irr_outside}

\clearpage
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\section{Sample Characteristics}
\label{app:2A:sample}

\input{snippets/representativeness}

\input{tables/tab_representativeness}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{fig_D_regional_representativeness.pdf}
  \caption{Regional distribution of interviewed and visited schools compared to
    the national population of open secondary and all-through schools in England.
    Bars show the percentage of schools in each region for the national population
    ($n = 3{,}312$), interviewed schools ($n = 300$), and visited schools ($n = 102$),
    the tier members matched to the school panel (the 2023/24 regional
    characteristics panel holds 3{,}312 of the 3{,}332 analysis-panel
    schools; the remainder lack a region record at that date). Regions
    are ordered by national share. Both samples over-represent London, and the interviewed sample the South East; the
    visited sample is particularly concentrated in London (45.1\% versus 15.0\%
    nationally). $\chi^2$ goodness-of-fit tests reject equal regional composition
    for both samples ($p < .001$).}
  \label{fig:2A:representativeness_region}
\end{figure}

\paragraph{Coverage of the text collections.} The three collections of
text were each gathered from the register of open state secondary schools at
a different date, which is why their raw counts differ. Inspection reports
were scored for 3,324 schools, behaviour policies were obtained for 3,363,
and websites were crawled for 3,370. Set against the analysis panel of 3,332 schools, the coverage counts and shares are those reported with the data: 3,272 reports, 3,294 policies and 3,311 crawls, of which 3,284 contain scorable prose. The few schools in each collection that
are outside the panel are schools that closed, merged or changed identifier
between the crawl and the construction of the panel.

\clearpage
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\section{Prediction Model Specification}
\label{sec:p1_app_prediction}

The text representation is TF--IDF over words and two-word phrases
(English stop words removed; terms kept if they appear in at least five
documents and at most ninety per cent of them; sublinear term frequency;
each document's vector normalised to unit length). The model is a ridge
regression with an unpenalised intercept, estimated exactly in kernel
form. Every reported correlation is leave-one-out: the model is refitted
with each school excluded, and the penalty is re-chosen within every fold
by closed-form inner leave-one-out, so the held-out school takes no part
in any fitting decision. Predictions are clipped to the range of the
training scores. Significance comes from a permutation test: the identical
pipeline is run on two hundred permutations of the outcome, and the
reported $p$ is the share of permutations matching or beating the real
correlation.

Before modelling, documents are cleaned of identifying and clerical
material: establishment, trust and local-authority names (from the
national register), month names and years, postcodes, tokens containing
digits, inspection-report page furniture, letter salutations, and
framework boilerplate. The deconfounded specification regresses each
outcome on the school's free-school-meals share, share of pupils with
English as an additional language, prior attainment and report age, and
takes the residual as the target.

Alternative representations were evaluated in the identical harness and none improved on the specification above. In brief: phrases of three and four words changed no correlation by more than noise. Topic groupings
performed worse than counting everywhere. Passage embeddings from two
pretrained encoders read enacted warmth and teaching at essentially
zero (the signal is vocabulary rather than paraphrasable meaning) while confirming the espoused-strictness result. One reading disagrees between the two encoders. The larger encoder reads enacted strictness at $+0.33$ with mean pooling, where the smaller encoder reads nothing and no word-based model finds it. Because it appears under one encoder and not the other, it is recorded here as unresolved rather than used.
Sentence-level annotations (topic and polarity tags) carried strictly
less information than the raw wording, and counts signed by sentence polarity raised the correlations but failed the chapter's rule of evidence: their gain came from clerical vocabulary and lifted every dimension at once, the signature of reading the document rather than the school.

\section{What the Models Read: Examples}
\label{sec:p1_app_examples}

This appendix supports the validity of the warmth and teaching prediction
models by showing their evidence in readable units. The models work on
weighted word and phrase counts, but a sentence's score is simply the sum
of its phrases' weights, so every sentence in the corpus can be ranked by
how strongly it pushes a school's score up or down. The listings below
give the strongest sentences in each direction for each model, followed by the strongest longer phrases (\cref{tab:p1_phrases}). The pattern is consistent: sentences
describing adults attending to pupils, and teaching designed and checked,
raise the scores; stock assurances and provision boilerplate lower them.
One oddity in the teaching listing should be read with care: several of
the score-raising sentences are criticisms (``pupils are not remembering
knowledge deeply''). The model counts the vocabulary of the
memory-and-retrieval discourse without registering negation, and a report only discusses long-term memory at all, approvingly or not, when the school works in that idiom. This blindness to negation is the same property that makes the model fail on strictness, where the negations carry the signal.

\input{snippets/app_examples}

\input{tables/tab_p1_phrases}

\section{The Marking Schemes}
\label{sec:p1_app_rubrics}

The seven schemes below are reproduced verbatim as frozen before scoring.
Each was applied once per document by gpt-4o-mini (version-pinned), which
returned a band and one quoted evidence sentence. Thirty documents per source were re-scored with caching disabled, with the agreement figures reported in the chapter, and the full grid was re-scored
once by gpt-4o as a sensitivity check, reported alongside and never used
to select results.

\input{snippets/rubric_schemes}
